### 2026-08-22 15:20 — Reviewed PR #30: merged (external ox-alpha writer engine)
Added `PODCAST_ENGINE=external` (`external_writer.py`, routes the two write passes through observer-system's `external_summon` to free `ox-alpha`, retry-then-Claude-fallback) alongside the existing `claude`/`gemini` engines. Verified all reused `or_writer.py`/`ingest.py` functions exist, CI green, scope matches the briefing. Squash-merged at `debb16bd9ff87e0bbf1db2803d4c702f0649eb6c` → `525fbb9`. Separately: this checkout's STATUS.md had ~190 lines of QC-log entries dating back to 2026-08-14 sitting uncommitted (never swept by the Stop-hook airbag) — committing them along with this entry rather than leaving them stranded.

### 2026-08-14 05:33 — QC on 08-14 script (mid-run): dedup ledger has a phantom "covered" story with no matching segment in the actual aired script, and the collision (again) is at a seam other than the two-pass join

Freshness/Dedup skeptic found `scripts/.covered-2026-08-14.json`'s `stories` array lists `nfl-vikings-kyler-murray-named-starter-2026-08-14` — zero mentions anywhere in the actual 370-line script. Root cause confirmed: this is the exact fabrication the 05:13 log entry (below) describes catching and cutting from the script earlier today — the covered-JSON's `segments` entry still has the full fabricated summary ("Vikings officially named Kyler Murray starter over JJ McCarthy"), meaning the earlier fix cut the script content but never reverted the ledger save. Removed both the `stories` slug and its `segments` entry directly from the JSON this pass. **New process gap worth fixing at the source:** when a QC pass cuts a segment for being fabricated/duplicate, the covered-stories save needs to be re-run or hand-edited too, or the ledger silently drifts from what actually aired. Separately, Coherence skeptic confirms (again, now the Nth time — see 08-02, 07-31, 08-07 entries below) that the two-pass front/back seam itself was clean; the actual BASIL/BASIL collision this episode was at the quick-hits→outro seam (lines 363/367) — fixed by flipping the outro's speaker tags and swapping which name each sign-off line claims, since flipping tags alone breaks the "I'm Brooke"/"I'm Basil" self-identification. Sourcing skeptic separately caught a CRITICAL defect of a new type: the Noah Smith "power vs. persuasion" segment (lines 317-323) had Smith's and Henry Farrell's actual positions reversed — Farrell (not Smith) is the one arguing persuasion counts as power deserving scrutiny; Smith's real, sourced argument is that conflating persuasion with power is definitionally sloppy and just relabels genuine product popularity as illegitimate. The closing "titans of industry" quote was real and stayed; the framing around it was rewritten to match the source. All three MUST-FIX items fixed and re-verified (zero speaker collisions across all 172 speaker tags on re-scan); QC VERDICT: PASS.

### 2026-08-14 05:13 — QC caught a wholly invented fact with zero trace to any source: "Vikings named Kyler Murray starter" (Murray plays for Arizona, not Minnesota)

Distinct fabrication type from prior logs (misattributed quotes, inflated stats, near-duplicate benchmark inversions — all *distortions* of real sourced material): this one had no connection to any cached transcript/article at all, just a plausible-sounding but factually impossible NFL depth-chart claim (Kyler Murray has never played for the Vikings) inserted into an otherwise well-sourced fantasy-football segment. The Coherence skeptic flagged it as "outside my mandate" but it was correctly escalated to MUST-FIX as unambiguously fatal rather than waiting for a second agent to corroborate — worth keeping that judgment call precedent: a single-agent flag of an impossible/checkable fact should be treated as fatal even off its assigned lane. Same episode also had a genuine sourcing distortion (Muse Glimmer benchmark claims inverted — script said it beat Qwen 3.6 27B, source says it scored lower, 35 vs 38) — both fixed. Two-pass seam and full speaker alternation were clean.

### 2026-08-09 04:56 — Coherence skeptic caught a new defect type: "tonight" leaking into a "Good morning" show's own outro recap

Mid-QC on the 08-09 script (still awaiting the Freshness/Dedup and Sourcing skeptics), the Coherence skeptic found the usual two BASIL/BASIL collisions across `[TRANSITION]` joins (NBA→Entertainment at line 265/269, Quick-hits→Outro at line 399/403 — consistent with the established pattern that collisions cluster in the back half, not the two-pass seam itself, which was clean again) but also a defect type not seen in prior QC logs: the outro's own recap line says "Tonight we end on SAP" inside a script that opens "Good morning" — a self-contradiction CLAUDE.md already warns against ("never say 'tonight' for a morning show"), but this is the first time it's shown up as the writer's own summary language drifting into evening framing rather than a greeting mismatch. Worth watching for on future outros specifically, since the outro is generated after the rest of the script and may not re-check tense/time-of-day against the opening.

### 2026-08-07 05:15 — second interactive QC pass on the same 08-07 script caught two wholesale-fabricated segments (invented quotes attributed to real journalists) that the 05:01 automated pass missed entirely

This was a genuine second, independent QC pass on the script already "fixed" by the 05:01 automated-pipeline run below — and it surfaced a materially more serious problem than anything the first pass caught. The Coherence skeptic confirmed the 05:01 speaker-collision fixes held (zero collisions across all 138 tags on re-scan) and the Freshness/Dedup skeptic re-flagged the `/doctor` build-pitch as a likely repeat of 08-02's pitch despite its explicit "this is a status update, not new" framing (logged as a judgment call, not fixed). But the Sourcing skeptic — checking quotes against the actual fetched transcripts in `.tmp/` rather than trusting the script's own citations — found two entire segments were fabricated outright: a "2024 WR draft class check-in" (lines 165-171) invented specific stats and quotes ("Madden brain," a Ben Solak report on Jaxson Dart) and attributed them to named Ringer Fantasy Football Show hosts, when the actual fetched transcript for that episode is about something else entirely (hardest players to rank) and none of the quoted material exists anywhere in the working tree. Same pattern for an NBA "Jokic/Curry/Draymond extension" segment (lines 211-219) attributed to a specific Bobby Marks Hoop Collective episode that was never actually fetched — dollar figures, trade-kicker mechanics, and quotes all invented. Both were cut and the surrounding transitions repaired (bridging line inserted for the NBA seam, since a straight cut would have created a BASIL/BASIL collision across `[TRANSITION]`). **This is the first time this project's QC has caught wholesale quote/segment fabrication rather than the usual dedup-repeat or speaker-collision defects** — worth treating as a signal that the automated 05:01 pass's own sourcing check (or the writer step feeding it) isn't reliably catching invented content when a topic *sounds* plausible and cites a real, named source. Two Ringer/Hoop Collective podcast episodes were apparently referenced by the writer without the actual transcript ever being fetched/available — worth checking whether the beat-reporter step is citing show names from the topic brief without confirming a transcript was actually pulled.

### 2026-08-07 05:01 — QC on 2026-08-07 script: the two-pass seam itself collided this time, plus two more elsewhere; fixed by inserting a bridging line instead of merging

Unlike the 08-02/07-31 findings (seam clean, collisions elsewhere), today the two-pass midpoint (line 145/147/149, AI/tech→sports) *did* have a BASIL/BASIL collision across the `[TRANSITION]`, alongside two unrelated ones (line 55/57/59 BROOKE/BROOKE, line 245/247/249 BROOKE/BROOKE) — confirms "scan every transition, not just the seam" is the right model, since the seam is not reliably safe either way. New fix technique worth keeping: for a collision straddling a `[TRANSITION]` (as opposed to two adjacent blocks within one continuous segment), *insert a short bridging line of the other speaker* rather than merging the two blocks — merging would wrongly erase a genuine segment break, and the qc-episode.md "merge, don't flip" rule was written for same-segment collisions, not cross-transition ones. Also: today's build pitch was a near-duplicate of 08-02's `/doctor`/CLAUDE.md-trimming pitch (same pattern as the 07-25 entry below) — fixed by reframing as a status update/escalation ("still unactioned, now overdue") rather than re-teaching the idea as new, which preserved the one genuinely new fact (build-cap gate reading over-cap for a month) instead of discarding the whole segment.

### 2026-08-03 04:09 — Build-pitch reporter found the fleet's own week-long Kalshi pagination bugs are a named, documented failure class ("silent tool-result truncation"), not a one-off

Cross-checking against `~/fleet-optimizer/STATUS.md`'s run of wrong position counts (five to eight in one night, 2026-08-01 entries — all traced to an unread `cursor` field or a first-page-as-total mistake) turned up independent academic (KDD-ETAAI'26, arxiv 2607.07405) and industry corroboration for the same pattern, plus a striking direct hit: Anthropic's own Claude Code hardcodes a ~25K-token tool-result cap (GitHub issue #45770, closed "not planned") — the exact cap the fleet independently hit reading its own `STATUS.md` on 2026-08-01. Pitched a shared deterministic verification-gate wrapper in `build-pitches/2026-08-03.md`, generic library auto-buildable, wiring into the actual Kalshi/copybot client flagged `money — discuss first`. Also verified-and-discarded Cole Medin's new QVAC (Tether local-AI SDK) video: real and well-built, but its local LLM backend tops out around Llama-3.2-1B-class quality, nowhere near GLM for the podcast's actual credit burn, so it doesn't clearly beat the already-known Ollama plan in `credit-resilience-plan.md`.

### 2026-08-02 04:45 — QC on 2026-08-02 script mid-run: again, the actual two-pass seam was clean but two other same-speaker collisions turned up elsewhere

Now a fourth occurrence (after 07-04, 07-31, and implicitly others) of the same pattern: the literal front/back-half join (line 131, AI/tech→sports) was alternation-clean, but collisions showed up at two unrelated internal seams — a BROOKE/BROOKE collision at line 65 (cold-open→featured-block, within the front half) and a BASIL/BASIL collision at line 275 (entertainment→economics, within the back half). The qc-episode.md prompt's framing ("pay particular attention to the half-join") keeps pointing at the wrong spot; the actual defect source seems to be *every* `[TRANSITION]` insertion point, not specifically the two-pass boundary. Worth eventually rewriting that instruction to say "scan every transition, the half-join is not specially at-risk" instead of implying it's the primary suspect. Separately, the Freshness skeptic flagged that `.covered-2026-08-02.json` has an empty `podcast_guids` list despite the script drawing on ~3 podcast episodes (Ringer FF Show, Fantasy Footballers, Dwarkesh/Sanderson) — not a script defect so not MUST-FIX, but a real gap in the dedup safety net for tomorrow's writer.

### 2026-08-02 04:42 — Today's ingest produced zero NFL/NBA/entertainment podcast transcripts; fell back to live web research for the sports/entertainment half

`.tmp/transcripts/` for 2026-08-02 only held two podcast files — Market Huddle (macro/FOMC) and an a16z Crypto episode (Clarity Act) — nothing sports- or entertainment-adjacent, despite the second-half brief explicitly calling for Ringer Fantasy Football Show / Fantasy Footballers / Barnwell quotes. Rather than force content from the wrong-topic transcripts or write thin fantasy coverage from the topic-brief's bare KD/76ers and F1 snippets, I used WebSearch/WebFetch to pull real, dated, dedup-checked material directly (Ringer FF Show tier episodes, Barnwell's trade-value guide, Fantasy Footballers' AFC North recap, ESPN/Deadline/THR entertainment items) — confirmed real by fetching source URLs, not fabricated. Worth flagging to whoever tunes the ingest/beat-reporter step: the podcast fetch for 2026-08-02 apparently only grabbed 2 episodes total and neither was sports/entertainment; if this recurs, the sports/entertainment beat has no local source material to write from at all.

### 2026-08-01 04:41 — QC on 2026-08-01 script mid-run: Gucci/Alpine F1 sponsorship is a same-facts repeat of 05-28 coverage, ~2 months back

The Freshness/Dedup skeptic caught this one only by reading `.covered-2026-05-28.json` explicitly — it's well outside any "last 48h" or "last 2-3 weeks" window, so it's a reminder that the dedup rule ("if covered in ANY previous script, skip it") really does mean checking the full covered-file corpus for recurring beats like F1 sponsorship news, not just a recency-bounded read. New quotes/anecdotes (Monaco pitch, attendance figures) don't make the core "Alpine gets Gucci as title sponsor" news fresh again. Coherence skeptic ran fully clean. Sourcing skeptic came back otherwise unusually well-grounded but caught two more MUST-FIX items in the same hedge-fund-collapse segment: a misattributed quote (Derek Thompson's Tesla/SpaceX comparison credited to podcast host Whittemore instead) and a fabricated precise age ("23-year-old" where the source only said "early 20s") — both classic small-fabrication patterns where a plausible-sounding specific detail got invented on top of real reporting. All three fixed directly in the script; re-verified with a full speaker-alternation scan afterward, zero collisions. Final verdict: PASS.

### 2026-08-01 04:10 — Build-pitch reporter hit fleet-optimizer/STATUS.md's own truncation live, and used it as evidence for the day's pitch (OKF / wiki indexing)

Reading `~/fleet-optimizer/STATUS.md` for the mandatory lens-check hit a hard 25,000-token read cap on a 1017-line, ~91,000-token flat log with no structured index — a real, freshly-observed instance of the "memory & state" retrieval-cost problem, not a hypothetical. Turned it into today's pitch (Google's Open Knowledge Format, `build-pitches/2026-08-01.md`), piloted on that same file. Also confirmed a dead end worth not re-chasing: Claude Code's `--max-budget-usd` background-subagent enforcement fix (2.1.217) and nested-subagent-depth default change (2.1.219) are both real (verified via `gh api` against GitHub's own release notes) but moot — `--max-budget-usd` is already the fleet's core per-call cost guardrail (`summon.py`, `consolidate.py`, `goal_judge.py`, `queue_accuracy_sweep.py`) and the fix is already live in the installed 2.1.220 binary.

### 2026-07-31 05:12 — QC on 2026-07-31 script: two-pass seam was clean, but two other BASIL/BASIL collisions appeared elsewhere in the back half

Same pattern as the 07-04 finding below (line 26): the qc-episode.md prompt's assumption that the two-pass front/back-half join is where speaker collisions live didn't hold here either — the actual writer seam (AI/tech→sports, line 195-199) was alternation-clean, but two independent BASIL/BASIL collisions turned up further into the back half, at the NBA→Entertainment transition and the Economics→Kalshi transition. Fixed both by merging the colliding segment-closing and segment-opening lines into one BASIL block ahead of the `[TRANSITION]` tag (per the qc-episode.md rule: merge, don't flip tags, to avoid cascading the alternation break downstream) — re-verified programmatically afterward with a grep-based adjacency scan, zero collisions remained. This is now two occurrences (07-04, 07-31) of collisions showing up away from the literal half-join; worth treating "seam-only" as the wrong mental model and instead always scanning the full speaker-tag sequence, not just the join point.

### 2026-07-31 04:39 — Build-pitch reporter closed an FO open loop (Claude Code 2.1.220, patched) and confirmed the "Loop Engineer" pattern this week's AI Jason videos push is already built here

Fleet Optimizer's 2026-07-30 scan flagged Claude Code 2.1.216's resume-permission-escalation fix as matching the fleet's dispatch shape but couldn't check the installed version from its sandbox. Ran `claude --version` here: **2.1.220**, four releases past the fix — also confirms the same release closed the git-worktree `--git-dir`/`GIT_DIR` escape vector, which narrows (doesn't eliminate) the justification for the still-pending Docker Sandboxes pitch (07-24/07-25). Full verification and a version-currency build sketch in `build-pitches/2026-07-31.md`. Separately: AI Jason's recent "Loop Engineer" video series (cross-loop shared signal files so one loop's discovery seeds another loop's backlog) is exactly `~/.observer/wiki/agent-os/compound-loop-signals.md` — built 2026-06-21 from a Killen Time pitch on 2026-06-18. Worth remembering so the next claude_lab scan doesn't re-research and re-pitch it when this creator's channel cycles back to the topic.

### 2026-07-25 04:23 — Confirmed from the podcast-writing side: today's build-pitches.md was a near-duplicate of 07-24's aired pitch, handled by trimming to only the genuinely new facts

Writing the 07-25 first-half script, `.tmp/build-pitches.md` pitched coordinator/executor tiering (same $81/$25.55 Berman numbers, same pilotfish tool) and Docker Sandboxes — both already stated on air in `killen-time-2026-07-24.txt`. This is the exact failure the 04:07 entry above just diagnosed on the reporter side (no check against recent `build-pitches/*.md` before pitching), now confirmed from the consuming end: without a fix upstream, the podcast-writing step is the only backstop, and it worked here only because dedup instructions explicitly say to skip already-aired facts. I didn't skip the segment outright — kept a shortened Build Pitch of the Day using only the two facts that were actually new (Anthropic's own 96%-at-46%-cost benchmark, AI Jason's persistent-subagent-session mechanic) and cut the repeated cost math and Docker Sandboxes entirely. Reinforces that the claude_lab beat brief fix proposed in the 04:07 entry is worth prioritizing — this is now two independent catches of the same gap in one production cycle.

### 2026-07-25 04:07 — claude_lab beat needs its own dedup check: today's two pitches nearly duplicated yesterday's exactly, from the same two videos

Wrote up two build-pitches for 2026-07-25 (model-tiered orchestration + Docker Sandboxes) before discovering, only at session-end while writing this entry, that `build-pitches/2026-07-24.md` already pitched the *same two ideas from the same two source videos* (AI Jason's tmux/Fable video, Matthew Berman's token-savings video, Cole Medin's Docker Sandboxes video) one day earlier — both still fell inside the `youtube.py --hours 168` triage window on consecutive days, and nothing in `claude-lab.md` tells the reporter to check its own `build-pitches/` history before pitching, the way `dedup.md` makes the podcast writer check `.covered-*.json`. Today's write-up isn't worthless (independently re-verified the same claims, added the persistent-subagent/SendMessage mechanism as a new implementation detail), but the beat brief should add an explicit step: before writing a new pitch, check the last several days of `build-pitches/*.md` for the same technique/source and either skip it, or explicitly frame it as "still unbuilt, re-confirming" rather than a fresh pitch.

### 2026-07-24 04:59 — QC on 2026-07-24 script: two dedup blind spots got past the writer, caught by QC verification against actual covered-*.json files

Two real cross-episode duplicates made it into the written script before QC: (1) JADEPUFFER ransomware story verbatim-repeated from 07-04 — the 20-day gap since the last episode (07-04→07-24) is well outside "read the last 48h," but `.covered-2026-07-04.json` still had it logged and should have been checked regardless of the 48h window; the writer instead logged it as newly "covered" again on 07-24, so the covered-file trail now shows it twice. (2) The AI Engineer World's Fair conference got re-mined for fresh quotes on 07-24 (Osmani, Shihipar, Brunet, Zach Lloyd/Oz, Garry Tan) despite being thoroughly covered across 07-02 and 07-03 — same "different quote, same consumed event" failure mode already flagged in the 07-04 entry below about retrospective framing, just recurring with a conference-recap source instead of a news retrospective. Fixed both directly in the script (cut/reframed ~35 lines), which dropped the episode to 13,293 words, under the usual 14-18k target — a real cost of catching this at QC time instead of at write time. Worth checking whether `/generate-brief` or the beat-reporter prompts scan covered-*.json by filename glob (which would miss anything outside a rolling window) rather than reading all of them regardless of date.

### 2026-07-12 01:54 — Reviewed PR #28: approved and merged

Fixed a real incident: `is_cos_session()` matched attachment *content* ("You are the COS"), and the observer-system switchboard CLAUDE.md (auto-attached to any session cd'ing into `~/observer-system`) contains the same phrase — silently voice-muting working sessions for life. PR swaps to matching the attachment *path* (`observer-system/cos/CLAUDE.md`). CI green, manual verification credible. Found and fixed in-place (4c) a stale doc: `code-voice/README.md`'s "hard-won design decisions" section still called the old content-match approach "false-positive-proof" — updated it so it doesn't contradict the code. Squash-merged fix/cos-mute-path-match → main.
### 2026-07-04 05:05 — "Month in review" framing device re-aired dedup-violating content; fixed and passed QC

The 2026-07-04 script's AI/tech opener used a Whittemore "what did June teach us" retrospective angle, which smuggled back three already-covered stories (06-07 token-scarcity/Walmart/Uber/Harvey, 06-10 Glean bot-sitting stat, 06-15 OpenRouter Fusion) dressed as new synthesis — dedup.md rule 3 ("a new take on the same facts is NOT new") doesn't get triggered by a source's own retrospective framing, so the writer waved it through. Rewrote to keep only the genuinely-new pieces (Artificial Analysis reindex, KPMG CEO-ownership stat) and reframe Glean explicitly as a callback. Worth flagging to whoever writes `/generate-brief` or the beat-reporter prompts: source retrospectives/recaps are a dedup blind spot distinct from breaking-news restatement.

### 2026-07-04 04:45 — QC on 2026-07-04 script: two-pass join was clean, but same-speaker collisions scattered elsewhere

Coherence skeptic found 4 BASIL/BASIL or BROOKE/BROOKE collisions across `[TRANSITION]` tags (lines 80, 91, 191, 279) plus a dangling trailing `[TRANSITION]` — but the actual front/back-half writer join (line 147→149) was alternation-clean. The qc-episode.md prompt's assumption that the two-pass seam is the defect site doesn't hold here; this looks like a broader speaker-alternation bug in the writing/assembly step, not something localized to the half-join. Worth checking whether `/assemble-episode` or the 2-pass writer has a systemic alternation bug beyond just the seam.

### 2026-07-03 14:22 — Reviewed PR #27: approved and merged

All three gates passed. Small (8-line), well-scoped plist change adding `StartInterval=1800` to `com.codevoice.flag` so the /tmp voice flag self-heals every 30 min instead of only at login; `restore-flag.sh` is idempotent/cheap so re-running it is safe. CI green (2x pytest). Squash-merged fix/codevoice-flag-selfheal → main.

### 2026-06-30 — Ornith-1.0 pitch is closer to "30 lines of proxy script" than an integration project — Ollama already installed with models running

Checked the actual Mac: Ollama is at `/opt/homebrew/bin/ollama` with `qwen2.5:7b` already running, and `run_task.sh` line 30 exposes `OBSERVER_TASK_RUNNER_CLAUDE` as the exact hook for routing dispatches to a different runner. The Fleet Optimizer's June 29 scan flagged the integration path as unverified; it's now clear the blockers are (a) write a ~30-line proxy translating Claude CLI flags to Ollama API calls, and (b) pull `ornith` (5.6 GB). Full pitch in build-pitches/2026-06-30.md.

### 2026-06-29 04:07 — Pre-negotiated "done" contracts is a distinct technique from the 06-28 adversarial-verification item — don't merge them

Today's build pitch (generator-evaluator with contract negotiation before building starts) and the 06-28 Fleet Optimizer item (N agents → tournament picks winner) are complementary but different: the 06-28 pattern asks "what's the right solution?"; today's asks "did we build what we promised?" before a single token of build work is spent. Implementation source: Anthropic AI Engineer Conference workshop transcript (https://www.youtube.com/watch?v=mR-WAvEPRwE) — this has more concrete implementation detail than the June 2 blog post the 06-28 scan cited, and should be the reference for anyone building either pattern.

### 2026-06-28 08:02 — Recovered 6/28 episode interrupted by reboot — PUBLISHED (spurious QC dedup overruled)

The 4am run rendered all 48 segments then the 07:12 reboot killed it ~1 min before finalize. Verified all 48 .tmp/segments intact (non-zero), ran gemini_finalize WITHOUT re-rendering (word count 6753 ≥ 6000 floor, so no regen). QC returned FAIL on DEDUP claiming the script is a "word-for-word duplicate of killen-time-2026-06-28.txt" — i.e. the file under review compared against ITSELF (Gemini Flash self-comparison hallucination; the corpus includes today's own script via or_writer's name-sorted glob). Disproved objectively: 0% verbatim-line overlap with 06-23/24/25/27 and entirely distinct lead stories vs 06-27. NOT a genuine content fail → published. Mixed existing segments via mixer.py (skipped voice.py, which deletes+re-renders), published via publish.py → release ep-2026-06-28-01 (Latest), live feed.xml carries 06-28. Removed stale logs/qc-FAIL-20260628-040006.flag. Feed: https://kylekillen.github.io/killen-time-podcast/feed.xml

### 2026-06-28 04:39 — Dynamic Workflows (Claude Code research preview) — monitor for GA before pitching

Anthropic launched Dynamic Workflows in research preview June 1: lead agent generates orchestration scripts on demand, fans out parallel subagents, saves progress for resume. Not pitched today because it's preview-only and the existing Workflow tool is more controlled for fleet production use. Re-evaluate when it hits GA — it would be the right pitch if it meaningfully simplifies the dispatch-then-fan-out pattern Kyle already runs.

### 2026-06-28 03:05 — Reviewed PR #24: approved and merged

All three gates passed. CI green (2× pytest). Decision logic covers all six branches, double-probe and cooldown guards are sound. Non-blocking: plist comment says "~3.5min warmup" but STATUS.md in same PR documents ~5.5min (math still correct; 600s cooldown >> 330s). Squash-merged feat/codevoice-watchdog → main.

### 2026-06-27 20:53 — Code Voice synth-server watchdog built (auto-restarts wedges)

New `code-voice/voice-watchdog.py` + `com.codevoice.watchdog.plist` (StartInterval 180s, installed/bootstrapped). It POSTs a tiny real synth to `:8765/v1/audio/speech` (20s cap) and restarts `com.codevoice.server` ONLY when the process is up + model warm + synth hangs — the wedge signature that `/health` can't see (health never takes the synth lock). Guards against restart storms: a single timeout is confirmed with a second probe (~40s of continuous hang before declaring wedged), warmup is recognized (port refused / `warm:false` → no restart), and a 600s cooldown stamp backstops everything. Verified all four states (ok / wedged / warming / cooldown) via mock + a live kickstart; decision logic has a `--self-test`.

### 2026-06-27 20:53 — Code Voice model reload is ~5.5min, not ~3.5min

Measured from server.log: a `launchctl kickstart` of com.codevoice.server takes ~4min to load the Kokoro mlx model and ~5.5min total to reach `warm:true` (e.g. 20:36:45 start → 20:42:15 warm). The port is connection-refused for that whole window. This matters for the watchdog: the 600s restart cooldown and the 180s probe interval both safely exceed the warmup, so a wedge-restart can't trigger a second restart before the server is back. It also means say_to_phone callers see ~5.5min of unavailability after any restart — its 4-retry/13.5s backoff does NOT cover that, so notes during a reload window are still dropped (pre-existing, not introduced here).

### 2026-06-27 20:53 — Root-cause note: server already serializes synth; the wedge is a stuck lock-holder, not a crash

server.py already added `_synth_lock` to serialize all mlx Kokoro GPU work (the prior failure mode — concurrent `model.generate` aborting the process — is fixed; see the long comments at server.py:127 and :294). Today's remaining wedge is different: one synth thread gets STUCK holding `_synth_lock` (model.generate hangs), so every other `/v1/audio/speech` blocks forever behind it while `/health` stays green. A bounded queue / reject-when-busy in server.py would make waiters fail fast (503) instead of hanging — a real UX win for say_to_phone — but it would NOT clear the stuck holder; only a process restart does. A true fix needs a timeout on model.generate, which mlx doesn't expose cleanly, so the watchdog is the pragmatic remedy. Left server.py unchanged (risky); flagging the bounded-queue idea for later.

### 2026-06-24 04:19 — `fallbackModel` missing from fleet; LiteLLM 1.82.7/8 compromised

Claude Code v2.1.166 (live on Kyle's v2.1.183) added a native `fallbackModel` chain — JSON config that auto-retries overloaded turns on a cheaper model — but it's not yet in `~/.claude/settings.json` or the alarm responder despite the Fleet Optimizer's June 22 model-pinning work. Important scoping note: `fallbackModel` handles 529 overloads and retired models only, NOT monthly-spend-limit exhaustion (that's the Gemini offload plan's job — two different failure classes). Also: official Claude Code docs include a security advisory that LiteLLM PyPI 1.82.7 and 1.82.8 were compromised with credential-stealing malware — flag before any LiteLLM integration.

### 2026-06-23 — Reviewed PR #21: approved and merged

All three gates passed. Adds Eventual (@Eventual-News, eventual.news) as a TOP TIER YouTube feed (weight 3.0, transcript_pull) and re-elevates the prediction_markets beat from 0 to 1 target segment, anchored on Eventual with the same editorial treatment as AI anchors. Clean minimal diff — 15 additions across feeds.json and beats.json, no unrelated changes. No CI configured (pre-existing condition).

### 2026-06-21 16:26 — Reviewed PR #18: approved and merged

All three gates passed. Introduces goal-seeking production loop (gemini_finalize.py): Phase 1 reaches render floor via per-segment regen with anti-shrink invariant, Phase 2 runs QC advisory-only and escalates via Telegram but never silently ships. gemini_qc.py refactored to detection primitive (no revise loop); source vs. freshness context separated and capped independently, fixing the 12K-truncation bug that made the grader fail every episode. Cross-segment awareness added to writer (prevents same-story repetition across segments). No CI configured.

### 2026-06-21 16:22 — Reviewed PR #16: approved and merged

All three gates passed. Pure 28-line tail addition to generate-episode.sh that emits compound-loop signals (covered/gap) after publish. All referenced variables ($RUN_ID, $RESULT_LOG, $SCRIPT_FILE) confirmed defined. Non-fatal guards correct. No CI configured on repo.

### 2026-06-20 04:49 — Second-half writer has no NFL transcript material; Ringer FF / Barnwell episodes not downloading

The four `.tmp/transcripts/` files available today were Dean Ball (Cognitive Revolution), Sam Vecenie (Athletic NBA Daily), Odd Lots panel, and Modern Wisdom. No Ringer Fantasy Football Show, no Fantasy Footballers, no Bill Barnwell episode was downloaded. The NFL segment had to be written from analytical framework knowledge rather than specific episode quotes — which is technically against the editorial rule of pulling actual source transcripts. Worth diagnosing why the sports podcast feed isn't producing transcripts; if those shows aren't in `feeds.json` as podcast feeds with working RSS, the beat's primary sources are invisible to the pipeline every run.

### 2026-06-20 04:34 — Speaker alternation fixes cascade badly when done one-at-a-time; fix all in one coordinated pass

Inserting expansion paragraphs via Edit during episode writing routinely creates consecutive same-speaker blocks. Fixing them one at a time shifts the problem downstream — each correction creates a new collision. The right approach is to grep the full speaker sequence first, map the entire broken cluster, then make all 5+ tag swaps in a single round.

### 2026-06-20 — youtube.py claude_lab scan returned 12 items with null IDs and empty transcripts — triage signal was blind this run

All 12 claude_lab items from `youtube.py --hours 168 --json` came back with `id: null`, no channel, no date, and no transcript snippet. The titles were visible but that was it. The dedup/ID problem in youtube.py made the primary triage signal useless; had to fall back entirely to yt-search skill + targeted web searches. Worth investigating why the ID field is null for the claude_lab batch specifically (could be a channel_id mismatch or a recent ingest dedup collision).

### 2026-06-19 17:35 — Claude Code fallback chains don't help the credit crunch (billing errors are explicitly excluded)
The `fallbackModel` setting activates on model overload/unavailability (529s etc.) but the official docs state billing and spend-limit errors **never trigger a switch** — they follow their normal retry/error path. The fleet's actual failure mode is a monthly spend-limit hit, not model overload, so configuring a fallback chain does nothing for the credit crunch. A prior Gemini-generated build pitch for today claimed the opposite; it was withdrawn. The right moves remain GLM offload (pending Z.ai key) and LiteLLM tiered-failover proxy (pending Kyle's approval).

### 2026-06-19 — Reviewed PR #14: approved and merged
All-Gemini engine wired into generate-episode.sh behind PODCAST_ENGINE flag; if/else structure correct, gemini_episode.py/gemini_buildpitch.py faithfully port the original methodology with covered-story saving preserved, deterministic _fix_joins replaces Claude QC. No CI configured (pre-existing); author validated end-to-end.

### 2026-06-19 — Reviewed PR #13: approved and merged
Turned thinking off for Gemini Flash content gen via OFFLOAD_REASONING_EFFORT; correct opt-in guard, coherent with existing OFFLOAD_* pattern, minimal diff. No CI configured (pre-existing).

### 2026-06-19 — Reviewed PR #9: approved and merged
Approved: OpenRouter completion helper (or_complete.py). Clean stdlib-only implementation, correct key fallback, solid empty-content guard for reasoning models. Fits podcast offload mission. No CI configured (pre-existing).

### 2026-06-19 12:39 — Reviewed PR #8: approved and merged
Approved: Telegram notification on publish. Non-fatal, suppressible, uses existing creds file, single call-site at the right chokepoint. No CI configured on repo (pre-existing).

# Killen Time — Session Notes

### 2026-06-18 11:48 — Speaker alternation violations cascade; fix by flipping parity in bulk, not one at a time

When a speaker tag is doubled in the script (two consecutive [BASIL] or [BROOKE] blocks), fixing the single violation creates a new one with the block that follows — and it cascades all the way to the next natural parity flip. The correct fix is to identify the section where the parity is inverted (everything between the first violation and the next same-type violation), then flip every tag in that section at once. Today: two inverted sections (lines 48–69 and 167–185), fixed with a single python pass that swapped BASIL↔BROOKE on all 15 affected lines simultaneously.

### 2026-06-18 — Observer-wiki is passive compilation; agent loops don't write signals for each other

Today's build pitch (compound loop signals) revealed a specific gap: Kyle's loops (podcast pipeline, COS, PR sweeper, dispatcher) are siloed — they don't proactively write structured discoveries for each other to read. The observer-system wiki compiles past events passively from the watcher, but no loop writes a signal saying "I just covered these topics" or "I found this gap" that another loop picks up on its next run. That's the missing connective tissue, and it's a low-cost add (plain markdown signals/ folder, 5-field schema, small hook additions). Build-pitches/2026-06-18.md has the full sketch.

### 2026-06-15 — Empty .tmp/articles/ is a hallucination multiplier; three segments were unsourced today

When article files don't make it to disk (today `.tmp/articles/` was completely empty — only podcast transcripts survived), the two-pass writer fills gaps with plausible but unverifiable content. Today's QC found three segments with zero source in the pipeline: a police officer/PoliceAI advisory center story, a Rio de Janeiro/Qwen fine-tuning story, and a Shane Legg four-paths-to-ASI paper — all cut. It also caught a fabricated prior-episode history ("we mentioned Headroom on June 13th") and a confirmed factual error where the transcript said "ten to fifteen percent" but the script said "eight percent." The fix: ensure article fetch (substack.py / ingest fetched URLs) actually writes to `.tmp/articles/` before the writer runs; if the directory is empty, the writer should be blocked or warned.

### 2026-06-17 — QC caught two covered-story violations the two-pass writer missed; half-join defects confirmed

The adversarial dedup skeptic caught that the McConkie/zero-RB Fantasy Footballers block (covered .covered-2026-06-15.json) and the India Telegram quick hit (covered .covered-2026-06-16.json) both slipped through the writer undetected — together ~15 lines of duplicate-covered content. The two-pass half-join produced exactly the two anticipated structural defects: a duplicate BASIL "pitch is logged" paragraph and a BASIL/BASIL collision across the Sports [TRANSITION]; both fixed by merging orphaned paragraphs into the preceding BROOKE block rather than flipping speaker tags. The dedup check is the most likely QC gate to catch real violations; the sourcing skeptic's MUST-FIX flags on Iran military figures and the Cherny quote primary source are the recurring attribution patterns to address in the writer prompt.

### 2026-06-17 — Context-compacted session continuation works for second-half writes; dedup nav on repeated topics

Writing the second half from a session-summary (after context compaction) worked without needing to re-read transcripts — the summary captured enough source detail (specific quotes, stats, segment angles) to produce ~8,100 words of fresh content. Key dedup nav this session: June 16 had covered Fox/Roku deal mechanics and Toy Story vs. Minions framing; today's second half covered the content-vs-platform philosophical angle (La Brea thesis, library-as-comfort-food, Netflix/Lionsgate data advantage) and Toy Story 5 specific tracking numbers — genuinely new angles on the same surface topics, so both aired without violation.

### 2026-06-17 04:29 — First half of June 17 episode written; GLM-5.2 is effectively the #1 open coding model with Fable banned

scripts/killen-time-2026-06-17.txt is complete through [TRANSITION] — 7,062 words covering AI/Tech (GLM-5.2, SpaceX/$60B Cursor acquisition, Databricks margin squeeze, token price index) and Agents (Whittemore training argument, Claude Code economics research, Zvi model welfare on Fable, goal-loop build pitch). Second half still needed: NBA, Entertainment, Economics/Culture, prediction markets quick-hit, outro. GLM-5.2 from Z.ai is MIT-licensed, 744B MoE, beats all Opuses on frontend coding — with Fable offline it's the practical top coding model available to practitioners right now.

### 2026-06-17 04:16 — Pipeline gap: generate-episode.sh has no QC-fail retry loop; QC verdict is a dead end

When QC returns FAIL the pipeline stops or continues to a weak episode — there's no mechanism to go back to the specific failing beat and retry it with fresh sources. The QC output already names the problem section; the missing piece is a loop wrapper in generate-episode.sh that re-runs only that beat, reassembles, and re-QCs. Pitched as today's build pitch (build-pitches/2026-06-17.md), anchored by Boris Cherny's "I write loops, not prompts" and Matthew Berman's June 12 Cursor demo.

### 2026-06-16 04:31 — Speaker alternation: merge adjacent same-speaker blocks rather than swapping tags

When fixing consecutive same-speaker violations in a long script, swapping a tag creates a new violation elsewhere because the surrounding blocks aren't balanced. The reliable fix is to merge the two adjacent same-speaker blocks into one (remove the duplicate tag, run the text together) rather than keep reassigning. This converges immediately; tag-swapping loops indefinitely.

### 2026-06-16 04:10 — Claude Code v2.1.178 adds `Tool(param:value)` permission syntax for model-tier cost routing

`Agent(model:opus)` as a deny rule in `.claude/settings.json` blocks Opus-tier subagent spawns project-wide — new as of June 15. IndyDevDan's same-day benchmark (15 app builds, $55 Sonnet vs $91 Opus) shows Sonnet matches Opus on 80% of tasks, making per-step tier enforcement worth wiring in. Beat reporters are the obvious first target. Build pitch in build-pitches/2026-06-16.md.

### 2026-06-15 — Ringer Fantasy Football Show transcript is stored as a single long line; offset reads fail silently

Podcast transcript files are sometimes stored with all content on one line — `wc -l` returns 1 and any offset > 0 fails with "file shorter than offset." The Ringer FF Show (podcast_7c8729f0-63cc-11f1-a99f-577680afcf05.txt) hit this today; the NFL segment was written from general knowledge rather than direct transcript quotes. When a transcript file reads as 1 line, read without an offset (the full line is there) or grep for keywords directly via Bash rather than using offset-based Read.

### 2026-06-14 05:06 — QC found two recurring two-pass footguns: section headers without TRANSITION, and quote-source drift across NBA show transcripts
Section header bracket tags (e.g. `[AI & TECHNOLOGY]`) placed directly after a speaker block — with no `[TRANSITION]` before them — get absorbed into that speaker's speech segment by the parser and will be read aloud; all section headers must either come after `[TRANSITION]` or be deleted entirely. Separately, the NBA Daily and Kevin O'Connor Show share some panel guests, causing quote-to-host attribution drift between shows ("Brunson burner" ended up attributed to Haberstroh from KO Show when it was DeFore from NBA Daily; LeBron/Spurs comp ended up on Simmons when it was the Hoop Collective floor); the fix is to attribute to show name, not host name, when the transcript has no clear speaker label.

### 2026-06-14 — NFL transcripts not cached; NFL segment must be written analytically or sourced differently
The Ringer Fantasy Football Show, Fantasy Footballers, and Bill Barnwell podcasts were not in `.tmp/transcripts/` today — only NBA, entertainment, and AI Daily Brief transcripts were available. The NFL segment was written from general mid-June offseason knowledge without direct quotes. If NFL pod coverage matters for the beat (it's the designated anchor), check `feeds.json` to confirm those feeds are configured and that the ingest pipeline is pulling them. This was the only structural gap in today's second-half production.

### 2026-06-14 04:30 — Agent Skills Progressive Disclosure is a greenlight-ready build (build-pitches/2026-06-14.md)
Anthropic's own platform spec for 3-level lazy-loading of skill context — metadata always in context (~100 tokens), skill body loads when Claude determines relevance, bundled scripts on explicit call. Verified: Simon Willison called it "maybe a bigger deal than MCP" at launch; OpenAI/Google/GitHub/Cursor all adopted the open standard within weeks of December launch. The build is: add YAML frontmatter to the existing .claude/context/beats/ files to convert them into Agent Skills, drop in skills directory, loading becomes automatic. Longer unlock is the skill creator pattern — Claude proposes skill updates during sessions, Kyle approves, skills improve from usage.

### 2026-06-14 04:09 — youtube.py --json misses URLs for claude_lab items; use yt-search directly
The youtube.py triage pass returned 14 claude_lab items with titles only — no URLs, no snippets, no published dates. The workaround that worked: search yt-search.py directly by title to recover URLs, then pull transcripts. Worth fixing upstream in youtube.py if the claude_lab beat keeps running.

### 2026-06-13 18:30 — ROLLBACK RECIPE for the AI-Layer change (PITCH 1 + PITCH 2)
The `.claude/` AI-Layer restructure + adversarial 3-agent QC were committed
directly to `main` (commits `db92260` → `e8f8504`, on top of merge `13e0bad`).
A pre-change anchor tag exists: `pre-ai-layer-2026-06-13`. To undo it all in one
line (keeps history, safest):
`git revert --no-commit 13e0bad..HEAD && git commit -m "revert AI-Layer" && git push`
Or hard-reset back to the tagged anchor (destructive, only if nothing newer
matters): `git reset --hard pre-ai-layer-2026-06-13 && git push --force-with-lease origin main`.
Note the tag points at the OLD long CLAUDE.md + single-agent QC, BEFORE the
STEP-0 branch reconciliation — so a hard reset also unwinds the chore-branch
merge. Prefer the `git revert` range, which undoes only the AI-Layer commits.

### 2026-06-13 18:30 — STEP 0: reconciled the lingering `chore/tmp-scratch-retention` branch into main
HEAD was on `chore/tmp-scratch-retention` (not a forgotten dead branch — it had
11 real commits origin/main lacked: all the Code Voice crash-fix work + the 98GB
disk-fill scratch sweep) and origin/main had DIVERGED (it carried the PR #2
squash of "shift emphasis" + a PR #3 review log the branch lacked). Uncommitted
edits were legit bugfixes (mixer.py 24kHz-mono resample; song_of_the_day.py
`/health` endpoint), not scratch. Resolution: committed the working tree in two
chunks, merged the branch into a refreshed main (4 config/code conflicts → took
the strictly-newer branch version; STATUS.md → unioned), verified main is a true
superset of all three lingering branches, deleted them local+remote. PR #4 (the
chore branch) auto-marked MERGED by GitHub once its commits reached main. Nothing
discarded. Clean main, tag `pre-ai-layer-2026-06-13` pushed as the anchor.

### 2026-06-13 18:30 — AI-Layer: CLAUDE.md 294→104 lines; QC is now adversarial 3-skeptic
Split CLAUDE.md into ~100 lines of true globals + `.claude/commands/` (5 pipeline
commands) + `.claude/context/` (per-beat guidance loaded on demand). The daily
`generate-episode.sh` keeps its proven 2-pass writer untouched; ONLY its QC step
(Step 3) was rewired to read+follow `.claude/commands/qc-episode.md` (the new
adversarial QC), so QC logic is shared with the interactive `/qc-episode` path.
Non-obvious: the generator delegates by instructing the agent to READ the command
file rather than relying on `/qc-episode` slash-expansion in headless `claude -p`
mode — more robust for launchd. Cold-start validation (a fresh agent) passed all
4 checks and incidentally caught a real `[OUTRO]` tag in today's script (an
illegal speaker tag voice.py would speak aloud) — evidence the QC spec works.
beats.json got a `context_file` field per beat so it is a true 1:1 source-of-truth
map to the context docs.

### 2026-06-13 — Reviewed PR #4: changes-requested (smallness gate)
Correctness and coherence both pass — cleanup-scratch.sh, code-voice thread-safety, Build-Pitch Reporter wiring, mixer.py resample fix, and song_of_the_day.py endpoint fix are all correct and coherent. Blocking on smallness: the PR bundles ~10 independent concerns under a title that describes only cleanup-scratch.sh. Fix: update PR title and body to describe all the changes included; no need to split.

### 2026-06-13 07:38 — Second episode rewrote killen-time-2026-06-13.txt; new lead is Amazon researcher attribution
The 4:42 AM first episode fully covered the Fable 5 shutdown. The 7:16 AM second run's new lead was the WSJ story (Amrith Ramkumar) naming Amazon researchers as the source of the jailbreak report, plus Luta Security CEO calling the restrictions a "complete overreaction" — genuinely new developments not in the first episode. The build pitch used Cole Medin's AI Layer (context rot → structured .claude/commands/ + on-demand context files), verified from build-pitches/2026-06-13.md written by the prior reporter session. Second half still needed from the pipeline's Pass 2 agent.

### 2026-06-13 07:26 — build-pitches file pre-existed today with a repeat of yesterday's loop engineering pitch
The `build-pitches/2026-06-13.md` file already existed (written by a previous reporter instance) with the same Boris Cherny loop engineering pitch as 2026-06-12 — exact dedup failure. Overwritten with two distinct verified pitches: (1) Cole Medin's AI Layer / `.claude/commands/` as a second codebase (verified via public GitHub repo + June 11-15 conference), and (2) adversarial 3-agent QC to replace the current single-checker QC step (Anthropic dynamic workflows, June 2, 2026). Also flagged: `youtube.py --hours 168 --json` returned 15 claude_lab items with empty URLs/channels/snippets — the YouTube data pipeline is not feeding claude_lab content properly and needs investigation.

### 2026-06-13 07:56 — Second-half Pass 2: no transcripts cached, no articles cached, NFL had zero new brief entries
Writing the second half of episode 2 required working entirely from the topic brief and background knowledge — `.tmp/transcripts/` and `.tmp/articles/` were both empty, so no podcast quotes could be pulled. The NFL beat had no new stories in the June 13 brief (all Barnwell all-pro content covered June 12), requiring a state-of-the-off-season framing without fabricating analyst quotes. Fresh angles found in two ESPN Wemby deep-dives (shot deterrence / Shaolin monastery profile) that were uncovered across all recent episodes.

### 2026-06-13 04:43 — NFL had no transcripts or topic-brief entries this week; NBA Finals is the sports anchor

No NFL podcast transcripts loaded this week and no NFL stories in the top 40 topic brief. The June 10 episode consumed the available NFL content (Barnwell first-team all-pro, trade rumors, specific players). When the calendar is in the OTA dead zone, lean entirely into whatever live sports is available (NBA Finals this week) rather than forcing thin NFL coverage. Resume NFL-led sports when training camp opens in late July.

### 2026-06-13 04:34 — voice.py reads `---` section dividers as literal TTS text
`parse_script` in voice.py includes any non-empty line that isn't a speaker tag into the current segment's text (line 56-57), so `---` dividers get appended and would be spoken aloud. Consecutive-speaker checks across section breaks are therefore real rendering bugs, not just cosmetic. Either strip `---` lines in `parse_script`, or use `[TRANSITION]` instead of `---` as the section separator in scripts.

### 2026-06-13 04:11 — generate-episode.sh has no retry loop on QC failure — that's the gap Loop Engineering closes
The podcast pipeline runs Claude steps linearly with no retry logic: when QC fails, the script logs an error and stops, waiting for human re-run. Boris Cherny's viral "write loops not prompts" framing names exactly this gap — the QC step already produces a clean pass/fail signal, so wrapping Pass1+Pass2+QC in a max-3-retry loop (feeding the QC report back as context on each retry) is the direct build. Pitched in build-pitches/2026-06-13.md.

### 2026-06-10 18:38 — Code Voice: concurrent turns crashed the TTS server (the REAL cause of "mixed results")
The dominant reason voice notes were inconsistently missing: mlx Kokoro is NOT thread-safe, and `server.py` uses `ThreadingHTTPServer` with synthesis done directly in each request thread. When two+ sessions end turns close together (overseer + forecast in parallel), their `/v1/audio/speech` calls hit `model.generate()` concurrently and **crash the whole server process**; launchd restarts it ~1s later but every in-flight request dies with `RemoteDisconnected` → no note for any of them. A turn ending in isolation delivered fine — hence "mixed." Detaching synthesis (the 17:50 fix) made it worse by letting children overlap. Reproduced cleanly: 3 concurrent children → all RemoteDisconnected + a `Code Voice server starting` line in server.log at the same second (crash+restart). Fixed with `_synth_lock` serializing all synthesis (3 concurrent → ok/ok/ok, no restart) + client retry/backoff in `say_to_phone.synth_mp3` + 120→180s client timeout. Debug tip: missing notes → check `server.log` for `RemoteDisconnected`/unexpected `server starting` lines (crash) vs `Broken pipe` (the older 15s-timeout bug). Ceiling: synthesis is now serial (~13-35s each), so ~5+ sessions ending simultaneously could still exceed the 180s client timeout for the last in line; if that bites, move to a proper bounded queue with longer waits.

### 2026-06-10 17:50 — Code Voice: long voice notes silently dropped (15s daemon-thread cliff)
The real reason long summaries "never became voice messages": `stop_hook.py` ran synthesis in a daemon thread with `t.join(timeout=15)`. Long notes (2-3k chars) take 20-40s to synth on the Kokoro server; at 15s the hook process exited, killed the daemon thread mid-request, and the socket broke (`server.log`: `[Errno 32] Broken pipe`) — no mp3, no send. Short notes (<15s) delivered fine, so it masqueraded as clipping. The earlier "speak the whole message" fix made it WORSE by pushing text past the 15s cliff. Fixed by spawning `say_to_phone.py` as a fully detached subprocess (`start_new_session=True`, text via stdin): hook returns in 0.45s, child synthesizes independently (verified 2376-char note → 37.6s → telegram ok=True, completes after hook exit, no broken pipe). Debug tip preserved in README: long notes missing → grep `server.log` for "Broken pipe" first. Open follow-up: many sessions firing detached helpers concurrently all hit the one shared (non-thread-safe) Kokoro model on :8765 — not observed failing, but a serialization/lock there may be needed if voice notes start corrupting under load.

### 2026-06-10 15:07 — Code Voice clipped to last paragraph; the Telegram path has NO summarizer
The voice-note clipping ("only the final sentiment reaches the phone") was `last_paragraph()` in `stop_hook.py:228` — `final_prose_text()` already returns the whole closing message, then we threw away everything but the final `\n\n`-separated block. Fixed to speak the full message (`spoken = strip_markdown(text)`). Non-obvious architecture fact for future debugging: the Telegram path is `stop_hook → say_to_phone → server.py /v1/audio/speech` and never touches the Ollama summarizer — that summarizer only runs on the OTHER endpoint, `/speak` + `afplay` (local Mac playback). So "the summarizer is dropping detail" is a false lead for Telegram notes. Verified end-to-end: the full 2,296-char sample message synthesizes to a 144s voice note (a clip would be ~15s). Also: `phone_hook.log` previously truncated entries to `spoken[:160]`, so a clip and a full message looked identical — now logs true length + paragraph count.

### 2026-06-10 04:30 — Transcript files sometimes bundle two separate shows in one file
Podcast transcript `podcast_9b79ba92-468a-4e8f-850e-dcf8e135b0c3.txt` contained both the Hoop Collective Game 4 Preview (Tim Bontemps/Ben McMahon) and the KOC show with Draymond Green concatenated into one file — both were recorded the same day (Tuesday afternoon post-Game 3). Future beat agents should read the full file and not stop at the first show's sign-off; the second show's content can be equally rich. June 10 second half is complete and appended to `scripts/killen-time-2026-06-10.txt`; covered stories and source archives saved.

### 2026-06-10 04:13 — Fable 5 shipped overnight; Anthropic says rewrite old CLAUDE.md files
Claude Fable 5 / Mythos 5 launched June 9th (the Polymarket market at 84% closed profitable). The single most actionable piece of Anthropic's release guidance: "rewrite old CLAUDE.md instructions and let the model use more judgment" — constraints written for Opus-class models may actively work against Fable's 9-hour autonomous style. First half of June 10 episode covers: Fable benchmarks + Mollick's "wizard to patron" firsthand account, silent RSI suppression controversy (0.03% of traffic silently degraded for frontier ML dev work), Glean Work AI Index (botsitting = 6.4 hrs/week consuming half of 13-hr productivity gain, 69% botshitting), and Zvi's analysis of OpenAI's RSI-by-2028 plan + the Achiam/Anthropic philosophy divide. Second half still needed.

### 2026-06-12 04:26 — Loop engineering is live in Claude Code today, sourced from its own creator
Boris Cherny (who built Claude Code at Anthropic) publicly said he no longer prompts Claude — he writes loops: a trigger + verifiable goal, and the agent runs until the goal is met. Claude Code ships `/loop` as a documented live command. Immediate application to this pipeline: replace the QC step in `generate-episode.sh` with a goal-directed loop that retries until QC passes clean, eliminating the current need for human re-intervention on QC failure. Full verified pitches (including Cloudflare's 7-specialized-agent code review pattern at $1.19/review) written to `build-pitches/2026-06-12.md` and `.tmp/build-pitches.md` for today's episode.

### 2026-06-09 04:38 — Two-pass episode QC: watch for duplicate [TRANSITION] at the join and unteaseed segments
Two bugs fixed in the June 9 script: (1) the two-pass writing workflow left back-to-back `[TRANSITION]` tags at the half-join, which would have fired a double audio sting — any two-pass episode needs this checked; (2) the Yglesias/Slow Boring consciousness piece was inserted mid-first-half with no intro mention, landing as a non-sequitur for listeners. Both patterns are easy to miss in a long script and should be explicit checklist items for future QC passes on split episodes.

### 2026-06-09 04:27 — June 9 second half complete; Ezra Klein Gaza/foreign policy is strong non-NBA beat for recurring use
The Ezra Klein/Matt Duss interview is among the richest economics/culture material the feed has produced — concrete data (anti-war candidate wins every election except 2004), specific named actors (Schatz, Van Hollen), and a structural argument about the Yemen-to-Gaza policy mistake loop that's hard to find elsewhere. This beat (Democratic foreign policy rupture) has staying power through the 2028 primary cycle and should be revisited whenever new developments hit. Also confirmed: splitting episode writing across two agents works cleanly as long as the second agent is given the first-half script to read — no duplication artifacts in this episode.

### 2026-06-09 04:17 — First half of June 9 episode written; second half ready to dispatch
`scripts/killen-time-2026-06-09.txt` has the intro + AI/Tech + Agents & Building beats (~7,500 words), ending with `[TRANSITION]`. Top stories covered: FrontierCode benchmark (Opus 4.8 at 13% on hardest "mergeable code" subset — significant recalibration of AI coding capability vs. SWE-Bench's 50%+ framing), WWDC results (Gemini-powered Siri + 20B on-device query-routed architecture — new development vs. June 7's preview), OpenAI S-1 filed today, China $295B data-center plan, AI consciousness debate (Yglesias vs. Chiang/Dennett), agent loops/state-machine debate from Latent Space, Agent Arena, AutoLab persistence finding, Hivemind, OpenEnv, and Tyler Cowen a16z messy-jobs frame. Second half needs: NBA Finals Game 3 results (Spurs won 115-111, Wemby 32pts), entertainment, economics/culture, one prediction-markets quick-hit if warranted, and outro.

### 2026-06-09 — Reviewed PR #1: approved and merged
Concurrency lock fix for double-episode problem. All three gates passed: lock logic correctly handles fresh/live/dead/stale-lock scenarios; step 4 direct python3 calls are coherent with the rest of the script; scope matches briefing. Pre-existing production drift committed alongside the fix (--model sonnet, retries, CODE_VOICE_MUTE=1, step 4 refactor) — author was transparent about this. Non-blocking note: artwork topics and publish description are now hardcoded strings rather than Claude-written; worth revisiting if episode art quality declines.

### 2026-06-08 05:29 — Two-pass episode writing leaves characteristic QC artifacts; add to checklist

When an episode is written in two separate passes (first half: AI/tech + prediction markets; second half: NBA + entertainment + economics), expect: (1) duplicate show intro from the second-pass writer not knowing the first already opened the show, (2) duplicate story mentions as the second writer covers front-office items already handled, and (3) analytical/thematic segments from the second half landing in the wrong section because they were appended after quick-hits rather than inserted into their topic block. All three appeared in today's 2026-06-08 script and were fixed in QC. The QC agent prompt should explicitly name these patterns when the script was written in two passes.

### 2026-06-08 — Second-half writing split across context window boundary; worked cleanly via summary
This session ran out of context mid-way through the second-half writing task and was resumed by a continuation agent reading the auto-summary. All four transcript files (Hoop Collective, Simmons, McWhorter/Coleman, Odd Lots/CoreWeave) and two Substack articles (Noah Smith Roundup 83, Nate Silver soccer) were successfully sourced, quoted, and archived. The split is invisible in the final script. Worth knowing: episodes at this word count (14,960 total) will reliably overflow a single context window, so second-half tasks should be spawned as independent agents from the start rather than continuations of the first-half session.

### 2026-06-07 05:31 — Second-half writing agent has no .tmp source material when ingest transcription hasn't run
The .tmp/transcripts/ and .tmp/articles/ directories were completely empty when this session ran, meaning the entire second half (NBA/Entertainment/Economics) was written from RSS topic-brief summaries only — no podcast quotes, no Substack deep-reads. The 60% podcast/Substack target in the editorial guidelines is structurally unreachable when transcription hasn't been pre-fetched. The generate-episode.sh pipeline needs to ensure transcript and article caching completes before handing off the second-half writing task, or the writing agent brief should explicitly note when no source files exist so it can signal the gap rather than silently proceeding with headline-only material.

### 2026-06-07 05:31 — June 6 episode was unusually comprehensive, creating heavy dedup burden on June 7 second episode
The June 6 script covered: full Game 2 NBA analysis (KAT/Wemby detail, Brunson fadeaway, Stephen A. Smith postgame), Zach Braff Modern Wisdom interview, Noah Smith Europe/China trade piece, Tyler Cowen Argentina/Thiel letter, Peter Attia PCSK9 gene therapy, Run-A-Muck/Drafting, BBC Studios creator partnerships, Madonna Confessions II, Apple TV EGOT/Schmigadoon, S&P 500 SpaceX rejection, GrapheneOS. When prior episodes are this comprehensive, subsequent episode NBA and Entertainment sections have fewer fresh angles and need to work harder to find genuinely new material rather than retreading covered ground.

### 2026-06-07 05:10 — Second episode AI/Tech must pivot to alignment/regulatory tier when all hard news is consumed
The first June 7 episode (published at 10:42 UTC / 4:42 AM MT) had consumed every major AI/Tech and Prediction Markets story in the topic brief. The second episode first-half session had no transcripts and no cached Substack articles. The working solution: pivot to the LessWrong alignment cluster (Neglected Basics/Against Corrigibility/belief-state tracking/activation verbalizers), the regulatory-tier stories (UK police court statement ban, ASML EU sovereignty, CXMT DDR5 parity), and use near-term events (WWDC, NBA Finals) as prediction market angles. This tier of content is always available even when the top-ranked news has been consumed — the alignment philosophy posts and regulatory stories recur weekly and should be treated as reliable second-episode material.

### 2026-06-07 — No entertainment/economics podcasts available; June 6 covered file had extensive NBA pre-coverage
The .tmp/transcripts/ directory had only AI and NBA podcasts (KOC Show, Athletic Draft Show, Cognitive Revolution, AI Daily Brief) — no entertainment or economics transcripts existed, so those second-half segments were built from RSS headlines only, which limits depth. Separately, the June 6 .covered JSON already had dense NBA Finals coverage (Game 2 full breakdown, CAT/Wemby quotes, Brunson fadeaway, Stephen A. postgame) meaning the June 7 second-half NBA segments had to find genuinely new angles (KOC "2007 LeBron" comparison, Mitch Johnson timeout critique, Harper minutes argument, 2026 draft guard class) rather than repeating what June 6 already stated on-air.

### 2026-06-06 05:28 — Two-pass scripts reliably produce double same-speaker blocks at seam
When a script is assembled from two independently written halves, the speaker tag alternation breaks at the join within each half — this session had consecutive [BASIL]/[BASIL] and [BROOKE]/[BROOKE] both in the FDA healthcare deep-dive, the most-written-into section of the first half. The QC agent must explicitly check for consecutive same-speaker blocks as a top-priority pattern, not just orphaned content or order issues.

### 2026-06-06 05:25 — .covered file can be pre-populated with ghost summaries before second half is written
The first-half agent called save_covered_stories() proactively with planned NBA/entertainment/economics summaries — so the .covered-2026-06-06.json already had detailed segment entries for Game 2, Wemby, KAT, Braff, etc. before the second half existed. The second-half agent should treat these as authoritative guides (what to cover, what quotes to use) not as proof coverage already happened — verify against the actual script before assuming a story is done. Also: no .tmp/transcripts/ or .tmp/articles/ files existed this run, so everything came from the topic brief and covered-file summaries; ingest pipeline likely didn't run before this session.

### 2026-06-06 05:12 — When previous pass already wrote a full episode, write fresh using uncovered stories
The june 6 file already had a complete episode from a ~10 AM pass; the covered JSON confirmed it. Rather than repeat or skip, the right move was to write a fresh first half from the remaining uncovered stories in the topic brief: Trump AI healthcare push (FDA fast-track / chatbots diagnosing/prescribing), LessWrong debate on software automation limits + Anthropic unilateral pause mechanism question, Zcash 2022 vulnerability disclosure, and Trump farmer roundtable as agricultural prediction markets entry point. All four were genuinely uncovered — the healthcare story had been mentioned in one sentence in quick hits but never developed. This is the pattern to follow when a "first half" task runs after a full episode already exists: audit the covered JSON, find the uncovered stories in the brief, build segments from those.

### 2026-06-06 04:50 — Two recurring QC patterns to watch in beat-agent scripts
Cold open / body stat duplication: when the intro agent writes a data-heavy cold open, the body agents tend to restate the same numbers verbatim rather than deepening them — QC should check that the body segment adds new framing or context rather than re-presenting cold-open statistics as if the listener hasn't heard them. BASIL→TRANSITION→BASIL: the second-half NBA sections had this pattern twice (rebounding→investigations, Atlanta/draft→entertainment) because section-closing and section-opening paragraphs both defaulted to BASIL; fix by splitting the closing paragraph off to BROOKE before the TRANSITION.

### 2026-06-06 04:28 — First half written; dedup boundary for second-half agent
`scripts/killen-time-2026-06-06.txt` contains: intro (Anthropic RSI cold open) + AI/Tech (Exa CEO a16z agent-search podcast, Anthropic "When AI Builds Itself" RSI numbers deep dive, Teleperformance short/Claude Code v2.1.166-167/OpenAI Lockdown/Google-SpaceX compute/Uber-Nuro quick hits) + Prediction Markets (Kalshi/Polymarket LA mayoral conspiracy theory sponsorship story, Susquehanna Odd Lots institutional adoption podcast, Polymarket "Argentina Model" Chris DeMuth Colombia/SpaceX-Cursor/GameStop-eBay). Script ends with [TRANSITION] — second-half agent must cover NBA Finals (Knicks 2-0 up, Brunson fadeaway Game 2), Entertainment, Economics/Culture, and write the outro. Do NOT re-cover any of the above; the Anthropic pause call angle (not the RSI data points) was covered June 5 so the June 6 first half covered only the new RSI numbers. Covered stories + source archiving still pending — happens after second half is written.

### 2026-06-05 04:46 — Hoop Collective transcript is one file containing multiple Ringer podcasts concatenated
The file `podcast_5c7365da-b8ea-11f0-b54e-4323677d71ed.txt` contains both the Hoop Collective Game 2 preview AND The Rewatchables mailbag in a single concatenated transcript — grepping by show name is required to find NBA content rather than reading from the top. Same concatenation pattern may apply to other Ringer feed transcripts; check with grep before assuming a transcript is what the filename suggests.

### 2026-06-02 04:22 — Backrooms/$81M A24 opening is genuinely new IP frame; second half structure used
The YouTube filmmaker moment (Backrooms + Obsession + Iron Lung in same five months) is now on-air — any future entertainment segment covering Kane Parsons, A24 box office records, or the "for us by us" Gen Z IP thesis should treat this as covered. The Yglesias "Triumph of Capital" piece (2012 divergence of asset returns vs labor returns) is fresh but now covered — the Anthropic founder wealth creation framing is the natural through-line to use in future episodes rather than re-explaining the piece. The Ezra Klein / Ian Bremmer "geopolitical incoherence" frame (Iran objectives unachievable, China policy inverted) is covered; Bremmer's tech-reversal observation (technology now advantages closed societies over open ones) is the non-obvious angle worth anchoring future foreign policy segments to.

### 2026-06-02 04:12 — Anthropic S-1 / IPO not yet covered; "token scarcity era" is now the dominant AI business frame
Anthropic's confidential S-1 dropped over the weekend ($47B ARR, ~$1T valuation, 130% quarterly growth) and was not covered in the June 1 or May 31 episodes — it's the biggest AI business story of the month and will need dedup tracking in future episodes once it's been covered today. The AI Daily Brief's May recap introduced "subsidy era → token scarcity era" as the structural frame for AI business model transition (flat-rate → per-token pricing across all AI products); this frame should be treated as established context in future segments, not re-explained from scratch. Maine Democratic primary is June 9 — Silver's structural finding of ~4-point Republican polling outperformance in Maine since 2014 is the non-obvious trading insight for that race.

### 2026-05-31 04:35 — QC catch: "tonight at midnight" vs "last night" tense trap for same-day deadline stories
Script said Copilot billing "switched at midnight last night" while also citing the June 1 effective date — a one-day tense error that would have aired as factually wrong. Pattern to watch: when a story is about a deadline happening tonight-at-midnight, the script writer often defaults to past tense ("switched," "arrived") because they're writing it as news. QC agent should explicitly check that "as of [date]" matches the tense used. Also caught "Kimmy" (Moonshot AI model name is "Kimi") — likely a Whisper transcription mishearing from the source podcast.

### 2026-05-31 — Second-half complete; Noah Smith pigs piece had unusually direct policy call-to-action
The Save Our Bacon Act (banning state-level gestation-crate laws) was buried in the Farm Bill that passed the House and is now before the Senate — Noah's piece ends with "call your senator," which is rare actionable content from a Substack source. Non-obvious dedup boundary: Game 7 game-time analysis (Cornett block, Chet 2-shot collapse, Haverstroh "experience is dead") was entirely fresh despite three prior episodes covering the series — the pre-game preview coverage from May 30 and the game-by-game recaps from May 28-29 left the *result and post-mortems* clean. The Vernor Vinge Meta-Golden Rule in Noah's pigs piece connected directly to the Vatican AI consciousness segment in the first half (same-week collision between "pigs deserve moral consideration" and "AI does not") — that through-line should be the episode's strongest thread if it lands.

### 2026-06-01 04:38 — Odd Lots transcript too large for Read tool; use Bash head to identify file contents
The commodity finance episode (podcast_9d19da5c) is a single-line file of ~31k tokens — the Read tool refuses it entirely and even `head -20` fails because the tool counts tokens not lines. Workaround: `head -c 500 <file>` via Bash reveals the first 500 characters which is enough to identify the episode. Any transcript that fails Read with "exceeds maximum allowed tokens" is a single-line file; use the Bash head trick. This blocked clean sourcing of the Odd Lots segment — had to write from topic brief excerpt only.

### 2026-06-01 04:21 — Transcript GUIDs don't match topic brief entries; scan all 10 manually before writing
Today's 10 available transcripts included Philosophize This (lying), Peter Attia sleep pharmacology, The Rewatchables (Spielberg/2001), Modern Wisdom (memory/language), Huberman Lab (peptides), and Prestige TV (Euphoria finale) — none of the AI-specific podcast GUIDs from the topic brief were findable by name. The AI Daily Brief (/goals primitive) and a16z Show (Happy Robot) were only identifiable by reading the first 50 lines of each file. Topic brief item #1 ("Building AI Agents for Enterprise Operations") pointed to a16z Show, but the matching transcript (podcast_0de90798) read as a16z only after reading the content. Pattern: always skim all transcripts early; don't trust topic brief transcript labels to tell you which file they map to.

### 2026-05-31 04:13 — Prediction markets beat was thin today; Vatican AI encyclical was the genuine lead
May 30's episode was so comprehensive on prediction markets (Rothera CFO comments, CFTC 6-state filings, AGA claim, Farooqui/Hormuz, ProphetX, American Power Index) that today's PM segment had to find fresh angles rather than new stories — the Circle/USDC blacklist court order and Robin Hanson's meta-institutions/futarchy essay were what was genuinely fresh. When the covered-stories file shows dense PM coverage from the prior day, expect PM to be thin and plan accordingly. The Vatican's 42,000-word AI encyclical (Pope Leo XIV, "Magnifica Humanitas") was the strongest lead — first major religious position on AI personhood, Anthropic/Vatican alignment paradox, White House EO kill — all sourced from the Moonshots podcast transcript. Non-obvious: today's 6 available transcripts were 4 NBA (game 7 post-mortems) + Cognitive Revolution (personal AI infrastructure) + Moonshots (Vatican/Deep SWE/EO). The NBA-heaviness was masked by the transcript filenames — worth checking transcript content early when building segment plans.

### 2026-05-30 04:34 — Two-pass join: last-speaker of first half must differ from first-speaker of second half
Today's join had BASIL closing the Farooqui/Hormuz prediction markets section AND opening the Game 7 NBA section, with only a [TRANSITION] between them. Fix was to split BROOKE's final Farooqui block at the "For prediction market traders:" paragraph — giving those two paragraphs to BASIL and reassigning the closing "broader implication" paragraph to BROOKE, so BROOKE bridges into the [TRANSITION] and BASIL opens cleanly on the other side. QC check to add explicitly: confirm `last_speaker(half_one) != first_speaker(half_two)`.

### 2026-05-30 04:13 — Rothera, Zvi system card, and Iran Hormuz all published after yesterday's episode cutoff
Three of today's richest sources — Event Horizon's Rothera article (2026-05-29T11:03), Zvi's Opus 4.8 system card breakdown (2026-05-29 ~20:49), and Polymarket's Math of Defeat Iran interview (2026-05-29 ~20:17) — were published after the May 29 episode finished (10:57 UTC). This is why the covered stories for 05-29 show "the-anthropic" as a slug without a segment summary: it was mentioned in the Innermost Loop section but the Zvi deep-dive didn't exist yet. Pattern to watch: evening-published Substacks (Zvi, Event Horizon, Polymarket Oracle) are almost always fresh for the next morning's episode regardless of date-matching.

### 2026-05-29 — Second-half appended; context compaction mid-session worked cleanly
Session ran out of context window after research but before writing. The auto-summary preserved enough — all transcript GUIDs, key quotes, dedup boundaries, and the append instruction — to resume and write the full second half without re-reading any source files. One non-obvious dedup call worth remembering: A24/Backrooms/Kane Parsons was covered 05-27 as backstory, so covering the *opening weekend box office ($9M Thursday previews)* today was clean — a new development, not a repeat. The "same topic, different angle" vs "genuinely new development" distinction is where dedup errors most often hide.

### 2026-05-29 04:22 — First-half script written; dedup boundary for second-half agent
`scripts/killen-time-2026-05-29.txt` contains the intro + AI/tech (a16z "$1B exits dead" / AlphaProof Nexus 9 Erdős problems / SpaceX $1.8T IPO + Blue Origin explosion) + prediction markets (World Cup futures / CFTC OMB review / Tennessee 6th Circuit / NC executive order / institutional investors). Second-half agent must NOT re-cover any of these; the Google/Polymarket insider trading case was covered thoroughly in May 28 Episode 2 and is also excluded. The script ends with [TRANSITION] at line 169 — second-half agent should append after that line, not overwrite.

### 2026-05-28 05:45 — Two-pass join reliably produces duplicate [TRANSITION] + consecutive-speaker violation in the NBA quick-notes block
Today's QC fixed: (1) a double `[TRANSITION]` at the seam where the two halves joined — always remove the extra one; (2) consecutive `[BASIL]` in the NBA quick-notes section (Wemby warning + Hawks note both tagged BASIL) because the second-half writer started the quick-notes block with the same speaker who closed the prior segment. The fix was to give Hawks to BROOKE and tag the Lakers note explicitly as BASIL, restoring alternation. The continuation-paragraph pattern (no tag = previous speaker) is intentional and correct throughout the script — don't add tags to continuation paragraphs or you create new consecutive violations.

### 2026-05-28 05:40 — First-half agent sometimes writes a complete episode, leaving the second-half agent nothing genuine to add
Today's first-half script included NBA, Entertainment, Economics, an outro, and a covered-stories file already fully populated — meaning the second-half task was "add more" to a technically finished episode. The right response was to identify stories genuinely not in the script (Google/Polymarket insider trading, Mistral European AI, Saudi rebate, climate tech IPOs) and append them after replacing the placeholder outro with [TRANSITION]. For future two-pass runs: the handoff prompt should specify a hard stop topic (e.g. "end after prediction markets") and instruct the first-half agent not to write the outro or save covered stories — both of those are second-half responsibilities.

### 2026-05-28 04:54 — Two-pass scripts produce 3 predictable sign-off artifacts beyond consecutive-speaker violations
Two-pass writing introduces: (1) time-of-day greeting mismatch in the outro (second-half writer writes in the afternoon and accidentally uses "Good afternoon" as the sign-off despite the episode being a morning show); (2) orphaned BASIL bridge lines ("Let's start with the commercial AI story.") left between the teaser and the first segment [TRANSITION], creating BASIL→TRANSITION→BASIL triples; (3) stories covered in the first half re-introduced as new in the second half (today: Illinois SB 315 fully explained in AI/tech, then re-established from scratch two segments later in the PM close). QC prompt should explicitly grep for same-time-of-day mismatches in intro vs. outro, short single-sentence BASIL blocks followed immediately by [TRANSITION], and first-half bill/company names appearing with "it's the first" or "the bill" framing in the second half.

### 2026-05-28 04:51 — Nothing worth recording
Session was a clean continuation: appended second half (NBA/entertainment/economics/quick hits/outro, ~8000 words), saved 27 covered stories + 7 podcast GUIDs, archived 7 source files. No surprises in the pipeline or content — all functions behaved as documented.

### 2026-05-28 04:24 — PM beat thinness solved via Manifold UK blog + Robinhood MCP when topic brief has only 1 PM item
Today's topic brief had 1 prediction market item (PredMTrader YouTube). Built a full two-segment PM section using the Manifold "An Orderly Succession" UK blog post (Burnham/Starmer/Makerfield by-election with specific market prices) and the Robinhood MCP announcement as a trading-angle story. Both are legitimately PM-relevant without being forced — UK political markets are genuinely undertraded vs. their analytical richness, and Robinhood MCP is a direct infrastructure change for traders. Pattern to reuse: when PM beat is thin, look first at Manifold's own blog and any "AI agents for trading" stories before declaring the beat weak.

### 2026-05-27 05:37 — Fixing a consecutive-speaker violation can create a new one in the adjacent block
When correcting a [BASIL]/[BASIL] pair by flipping the second to [BROOKE], if the block after it is also [BROOKE], you've just created a [BROOKE]/[BROOKE] pair. The fix introduced exactly this: changing Japan ramjet from [BASIL] to [BROOKE] resolved the WeRoad/Japan issue but created a new Japan/BenchBench violation. Sequential speaker-fix passes need to re-scan from the corrected point forward, not just verify the original violation is resolved.

### 2026-05-27 05:05 — Two-pass episodes produce 8+ consecutive-speaker violations, not just 1 at the seam
The join point is the obvious problem but not the only one: the second-half writer doesn't know which speaker ended each first-half segment, so the same violation propagates at every transition throughout the second half. Today's script had 8 consecutive same-speaker errors (vs. the 1 at the seam you'd expect). The QC agent should run an automated speaker-sequence check across the whole script, not just around the [TRANSITION] join. Also: when two half-agents independently cover a data point (today: Ampere TV-season-gap stat at both line 263 and 363), each uses it in a different analytical frame — not pure duplication, but the raw finding restated verbatim. QC should grep for repeated proper nouns + numbers within the same script and flag them.

### 2026-05-26 04:49 — Two-pass QC failure modes: duplicate sign-offs, duplicate quick-hits coverage, same-stat-different-context from two sources
Two-pass scripts reliably introduce: (1) duplicate section-closing action items when both halves want to close a topic with the same sentence (e.g. "Watch the Finals odds markets…" appeared in both the PM section and the NBA section); (2) topics covered fully in a main section re-appearing in quick hits (BTS/AMAs covered in entertainment and again verbatim in quick hits); (3) the same statistical claim attributed to two different podcast sources measuring different contexts, which reads as contradictory on air (KOC's 24% vs Group Chat's 67% for SGA paint rate — both correct, different contexts, needed a clarifier). QC agent prompt should explicitly flag these three patterns.

### 2026-05-26 04:41 — Nothing worth recording
This resumed session only ran two Python cleanup calls (save_covered_stories + archive_used_sources) after the previous context window wrote the full episode. Execution was clean: 24 stories saved with full summaries, 8 podcast GUIDs marked consumed, 8 transcripts archived. No surprises.

### 2026-05-26 04:19 — First half complete; PM beat thinness solved via general-election downstream trade + Finals odds
Today's topic brief had zero prediction markets stories (0 of 40 items). Solution: use Texas runoff *results day* as a trading-implications discussion (the primary at 95% is priced in; the trade is watching the general-election market reprice in the first hour after Paxton wins), and use NBA Finals odds as the second PM segment. This "downstream market repricing" framing is a valid editorial approach when no dedicated PM stories surface — it satisfies the trading-focused editorial principle without forcing thin primary-market content. Also confirmed: Innermost Loop articles timestamped late evening (11:31 PM) are reliably fresh for next-day episodes since the daily show runs ~11am UTC; always check the Innermost Loop timestamp before deduplication.

### 2026-05-25 05:10 — Full episode complete; Edit tool fails on repeated [TRANSITION] strings — use surrounding block as anchor
When appending a second half to a script that already contains multiple `[TRANSITION]` markers, the Edit tool returns "Found N matches" and aborts. Fix: use the full content of the final [BROOKE] or [BASIL] block immediately before the target [TRANSITION] as the `old_string` — it's always unique. Also confirmed: `archive_used_sources(used_article_paths=[...])` DID work this session (unlike May 24) — the 2026-05-25 episode archived 6 transcripts + 1 article without any manual mv needed, so the earlier bug may have been a path issue rather than a code issue.

### 2026-05-25 04:12 — First half complete at ~7,800 words; CFTC capture story is the day's key prediction markets angle
`scripts/killen-time-2026-05-25.txt` covers intro, AI/Tech (a16z Ramp Economics SaaSpocalypse-not-in-the-data with Ara Karazian quotes + AIDB Dan Schipper "After Automation" / Eternal Sloptember; Pope Leo XIV AI encyclical released today + Meta/Google/Amazon April 29 Vatican lobbying; Reid Hoffman "Reid AI" 75+ addresses; Boz Bosworth Meta workforce transformation; Huawei IEEE 1.4nm chip scaling law; DeepSeek HBM reduction enabling Chinese domestic hardware ecosystem; Xiaomi $8.8B AI; UK neuromorphic; security engineer demand +11% YoY), and Prediction Markets (NYT CFTC gutted by Trump admin / Trump family in crypto+prediction markets — full trader-lens analysis: near-term protection may be MORE robust due to conflict of interest, medium-term politically fragile, weakens Kalshi's federal preemption narrative in Rhode Island case). Ends at [TRANSITION]. Second half still needed (NBA, Entertainment, Economics/Culture, outro). DO NOT save covered stories or archive sources until both halves complete. Note: Anthropic/Vatican/Olah-invited was covered May 24; today's encyclical content (actual doctrinal document released) is genuinely new territory.

### 2026-05-24 04:36 — QC: two-pass scripts can produce false in-episode callbacks referencing content that was cut
When the second-half agent writes "We talked this morning about the Epoch AI finding" but that finding never appeared in the first half, the callback is a phantom reference that reads as a continuity error on air. The QC agent should grep for phrases like "we talked," "we covered," "as we mentioned," and "from earlier" and verify each one actually exists in the script above it — they're common artifacts of second-half agents that assume first-half content they didn't receive.

### 2026-05-24 — Second half complete; archive_used_sources does not move articles — manual mv required
`archive_used_sources(used_article_paths=[...])` silently does nothing for article files — only the transcript GUID archive actually moves files. Had to `mv` the 5 used articles to `.tmp/used/` manually. Also: no NBA or entertainment podcast transcripts were available for this episode (only MacroVoices and Moonshots, both consumed by the first half) — the NBA and entertainment segments were written from ESPN/Deadline/Hollywood Reporter data without podcast quotes. Future NBA episodes should check for Ringer/Real Ones/Athletic transcripts before writing; their absence degrades segment quality noticeably.

### 2026-05-24 04:11 — First half complete at 6,981 words; Moonshots podcast $15B Anthropic/SpaceX figure is podcast-reported, not independently verified
`scripts/killen-time-2026-05-24.txt` covers intro, AI/Tech (DeepSeek V4 Pro 75% price cut permanent, Moment AI $78M, Salesforce Agentforce mock-up deception, Princeton Digital $1B China data center exit, quantum-crypto threat FT, SpaceX IPO prospectus specifics incl. Anthropic $15B/year for Colossus 1+2, XAI pivot to infra, Cursor acquisition 30 days post-IPO, Google Docs Live, Sarama interspecies dog-collar AI from Innermost Loop, Anthropic/Vatican/Pope Leo encyclical, AI pirated audiobooks), and Prediction Markets (Texas Senate runoff Tuesday — Paxton 60%→95%, Nate Silver WSOP 26-tips guide, FutureSim/GPT-5.5 beating Polymarket Super Bowl crowd, Manifold changelog bonuses/Uncertain filter/self-block). Ends at [TRANSITION]. CAUTION: the $15B/year Anthropic-pays-SpaceX figure comes from the Peter Diamandis Moonshots podcast (ep. 257, May 22) citing the SpaceX prospectus — it has not been independently verified against the actual prospectus document; use hedged language if re-citing. The MacroVoices podcast (podcast_45b5d053) is a macro trading interview with Tony Greer, NOT a prediction markets or AI podcast — it was used lightly for texture but should not be archived as a primary prediction markets source. Do NOT save covered stories or archive sources until both halves are complete.

### 2026-05-23 04:13 — First half complete at 7,146 words; AI Daily Brief weekly recap transcript overlaps heavily with May 22 — do not re-use
`scripts/killen-time-2026-05-23.txt` covers intro, AI/Tech (ForecastBench LLM beating superforecasters, Qwen3.7-Max 35hr autonomous run, Kemira/CuspAI PFAS chemistry, retatrutide gastric-bypass-matching trial, EngineAI 10K humanoid line, ClickUp/AT&T/Newsom labor angle, Project Glasswing 10K vulns, Zoom Anthropic stake $1.27B/$380B, Cursor Composer 2.5, Cloudflare CEO middle-managers thesis, NTSB voice-clone database suspension, SolarWinds/Treasury FOIA docs, Sundar Hard Fork persistent agents, Zvi on Gemini 3.5 Flash sycophancy), and Prediction Markets (Gouker Event Horizon "Stop betting on Survivor" full analysis with Probst quotes, BBC young-men demographic, mention markets critique, RI AG vs Kalshi dueling lawsuits, 9th Circuit stay denials, India blocking, 41-AG CFTC letter, NHL-CFTC MOU, congressional staff bans, Kalshi retail ETF/TOLL, NPR Throughline history context). Ends at [TRANSITION]. The AI Daily Brief weekly recap transcript (`podcast_2938d95c-faf6-4f86-b78c-ffeef181c741.txt`) covers Karpathy/Anthropic profitability/Cerebras/Erdős already aired in May 22 — do NOT use it again. Fresh NBA transcripts available: Real Ones Game 3 + Athletic NBA Daily Game 3 (both unused). Do NOT save covered stories or archive sources until both halves are complete.

### 2026-05-22 04:37 — Two-pass consecutive-speaker hotspots: intro area and quick hits section are the recurring culprits
Yesterday's STATUS entry identified the consecutive-speaker problem in general; today's QC pinpointed where it reliably appears: (1) the show intro, when both the pred-markets teaser and the formal "This is Killen Time / I'm Basil" line land on the same host in the same pass, and (2) the quick hits section, where rapid-fire items on unrelated topics often stack onto the same speaker without a natural handoff cue. Both locations should be the first places a QC agent greps when checking for `\[BASIL\].*\n\n\[BASIL\]` patterns.

### 2026-05-22 04:34 — Erdős dedup: two distinct events, same problem name, one day apart
The May 21 covered file slug "erdos-unit-distance-problem-solved" refers to Cédric Pilatte (high schooler solving it with AI assistance — human-led). The May 22 second-half covered a separate event: OpenAI's general-purpose AI independently disproving the same conjecture without human direction. Both are now covered. Future producers seeing any Erdős unit distance coverage should check BOTH covered summaries before deciding it's new content.

### 2026-05-22 04:19 — First half complete at 7,001 words; prediction markets beat is thin today — almost everything was covered May 21
`scripts/killen-time-2026-05-22.txt` covers intro, AI/Tech (killed AI EO + open source debate + Clem Delangue a16z podcast + Scott Alexander "New Paradigms" + Karpathy/Anthropic profitability/OpenAI IPO/Cerebras No Priors/Google I/O), and Prediction Markets (EPA refrigerant market, AI EO implications, Binance-Iran adjacent risk). Ends at [TRANSITION]. The pred markets beat is thinner than usual because May 21 consumed almost all of the significant stories (NJ SCOTUS, CFTC vs Minnesota, Polymarket private markets, Kalshi volume) — today's only fresh pred-markets content is PredMTrader's EPA announcement market ($100K volume) and the EO kill as a regulatory signal. Do NOT save covered stories or archive sources until both halves are complete.

### 2026-05-21 05:08 — Two-pass QC: section headers don't break consecutive-speaker rule; watch for time-of-day and story duplication at the join
In a two-pass script, the second-pass writer tends to (1) open the NBA section with "Good afternoon" not knowing the episode was generated at 4 AM, (2) re-cover a story already in the first half's quick entertainment section (Poirot appeared twice), and (3) leave orphan speaker tags at segment boundaries where section headers (---) visually imply a break but the podcast has none — two consecutive [BASIL] or [BROOKE] blocks separated only by a heading still violate the alternating-speaker rule. QC agents should specifically grep for consecutive same-speaker tags across the full file, not just within named sections.

### 2026-05-21 05:03 — Full 5/21 episode complete (11,826 words); podcast_6a0ed36cd7997e788c43a882 is NOT an NBA Daily Brief
The second transcript file with that GUID is actually The Economist Intelligence podcast feed, not an NBA Daily Brief — the prior context summary misidentified it. It was included in archive_used_sources but wasn't actually used in the script. If it turns up archived incorrectly on 5/22, restore it from .tmp/used/. The actual NBA second-opinion source for Game 2 was the Ringer Group Chat (`2c3b9e36-54ef-11f1-adc8-6f54093c7dc9`).

### 2026-05-21 04:52 — First half complete at 7,024 words; second pass needed for NBA/Entertainment/Economics + outro
The 5/21 first half covers AI/Tech (Erdős proof, Noah Smith roundup, Jaan Tallinn Manifold pod with extensive direct quotes, METR rogue deployment report, Karpathy to Anthropic, Robin multi-agent biology, Manus buyback, AMD/Samsung/SpaceX hardware) and Prediction Markets (Minnesota criminal felony + CFTC same-day suit, NJ→SCOTUS joint status report, Polymarket+Nasdaq private company markets, Betr NFA registration, Trump commencement speech markets). Ends at [TRANSITION]. Do NOT save covered stories or archive sources until both halves are complete.

### 2026-05-20 04:18 — First half came in at 5,382 words; second pass must run long to hit total target
The 7,000–9,000 word target is for the first half alone, but the 5/20 first half landed at 5,382 words — about 1,600 short. The second pass (NBA/Entertainment/Economics/outro) needs to compensate; aim for 9,000–10,000 words in that half so the full episode clears 14,000. The shortfall came from tight dedup constraints: May 18 and May 19 had already consumed most obvious AI and prediction-markets material, forcing narrower coverage than a cold-start episode would allow.

### 2026-05-18 04:33 — Two-pass scripts need intro updated after second-half is appended
When an episode is generated in two passes, the intro is written in pass one and covers only the first-half beats. After the second half (NBA/entertainment/economics) is appended, the intro must be updated to preview those beats — otherwise listeners hear a show that promises two topics and delivers five. The QC agent is the right place to catch this: check that every topic section in the script has a corresponding line in Brooke or Basil's opening overview.

### 2026-05-18 04:29 — Episode split across two sessions works cleanly; [TRANSITION] is the reliable splice point
The killen-time-2026-05-18 episode was generated in two separate Claude sessions (first half in one, second half in another after context compaction). The pattern worked: the [TRANSITION] tag at end of the first half is an unambiguous append anchor for Edit tool. Transcript identification requires reading first 20-40 lines since filenames are GUIDs — no shortcut. The NBA Daily GUID uses no-hyphen format (`6a0ad326fa12c3b85c4f50dd`) while others are hyphenated UUIDs; both work fine with `save_covered_stories`.

### 2026-05-19 04:09 — May 18 saturated the standard AI/tech beats; pivot to defense/drones + European ecosystem worked
The 5/18 episode had already covered Mythos, Grok, Griffin, Palo Alto window, CXMT, NV Energy, Epoch AI, Tasklet, Malta — nearly every obvious AI story. For 5/19, the AI beat pivoted to Noah Smith's drone warfare piece + Saronic/a16z shipyard podcast (both genuinely fresh) and the European AI ecosystem (Mistral-Emmi, Lexroom, ex-DeepMind $14B). When a prior episode is this thorough, check the honorable-mention rows in the topic brief and the Substack full articles for angles the Techmeme headlines don't surface.

### 2026-05-19 04:07 — Prediction markets regulatory siege hit five simultaneous vectors in one week
AGA+IGA letter to Congress, NFL letter to CFTC, Senate unanimous ban, 56 House Democrats to DOJ, Bubblemaps $2.4M Iran-war bets with 98% win rate — all dropped in the same 72-hour window. Event Horizon Episode 17 is the cleanest single source aggregating this. The Kalshi $585M Saturday / 86.6% sports figure is the provocation; the gaming lobby response is the story.

### 2026-05-18 04:13 — May 15 covered JSON has empty segments{}; Innermost Loop articles are the real AI/tech spine
The `.covered-2026-05-15.json` file has an empty `"segments": {}` dict, so future writers can't use it for fact-level dedup — they must read the actual May 15 script. More broadly: the Innermost Loop daily articles in `.tmp/articles/` (May 15–17) are the richest AI/tech source this cycle, far denser than the Techmeme links in the topic brief, which require separate WebFetch to get full content.

### 2026-05-15 08:53 — `claude -p` hangs after completion is the silent failure mode for May 13–15

Three days of missed episodes (May 13, 14, 15) all share the same surface symptom: write-pass1 times out at 1800s twice and the script aborts. But today's failed run actually wrote a complete, well-formed 7007-word first half to `scripts/killen-time-2026-05-15.txt` (ends correctly at `[TRANSITION]`) — claude-p **finished the work and then hung indefinitely** instead of exiting. The 1800s timeout killed the process AFTER the work was already on disk. The retry then redid the same work and hit the same hang. So both "TIMED OUT" lines in the log are completions-followed-by-hangs, not actual generation failures.

Diagnostic: `claude -p` with a trivial PONG prompt returns in 5–10s, including under a stripped-down launchd-equivalent env. The hang only manifests on the real episode prompt (large prompt + heavy file I/O + tool use). Likely a CLI teardown bug — possibly worsened by the 2026-05-14 update to Claude Code 2.1.142. May 13 was a separate cause (org monthly usage limit, now resolved).

**The script fix:** `run_claude_step` in `generate-episode.sh` needs a stable-size detector. Watch the target script file; if size hasn't changed for ~4 min after at least 5 min of running, kill claude and proceed — the work is done. The current "wait for claude to exit or hit hard timeout" pattern wastes the entire 1800s budget on a completed-but-hung process. See `.tmp/run-pass2-recovery.sh` for the recovery wrapper that demonstrates the pattern. Also worth shipping: if the hard timeout DOES fire but the script file is well-formed (ends at `[TRANSITION]` for pass1, contains an outro for pass2), DO NOT retry — accept the work and proceed.

Recovery this morning: pass1 output is already on disk from 04:31, so today's recovery is pass2 → QC → render → mix → publish using the existing partial script. Topic brief and 21 articles + 44 transcripts in `.tmp/` are still intact from the 04:00 ingest.

### 2026-05-12 05:52 — Section headers don't break same-speaker runs; merging beats swapping tags
Two [BASIL] blocks separated only by a bare section header (e.g. "ENTERTAINMENT AND MEDIA") still count as consecutive — the header is not a speaker-break. The cleanest fix when two same-speaker blocks are adjacent at a section open is to merge them into one paragraph rather than flip the tag, because flipping cascades the conflict into the next pair. QC should treat section headers as invisible to speaker alternation.

### 2026-05-09 05:41 — Fixing a segment-boundary tag flip cascades through the entire segment; [TRANSITION] separates same-speaker pairs acceptably
When you fix a consecutive same-speaker issue at a segment boundary by flipping the first speaker, every subsequent speaker tag in that segment must also flip to maintain internal alternation — fixing just the first pair pushes the conflict inward. Separately: same-speaker blocks separated by a `[TRANSITION]` tag are acceptable (the transition is a genuine section break); only flag same-speaker across plain `---` dividers. Two-pass scripts consistently produce the cascade pattern at both join points.

### 2026-05-07 05:31 — Two-pass script writing reliably produces consecutive same-speaker blocks at segment joins
When separate agents write different halves of a script, the second pass almost always starts its first segment with [BASIL] regardless of what the first pass ended on — producing consecutive [BASIL] or [BROOKE] blocks at the seam. Today: Segment 3 closed [BASIL], Segment 4 opened [BASIL], requiring 4 cascading tag fixes. The QC agent should explicitly check the tag immediately before and after every `---` segment divider, not just within segments.

### 2026-05-07 05:25 — Large transcript files require python3 string-search, not head/grep
The .tmp/transcripts/ files are unnamed (GUID-based) and can exceed 100KB, causing head/grep to truncate or return unhelpfully large output. Effective pattern: `python3 -c "with open('file') as f: text=f.read(); idx=text.lower().find('keyword'); print(text[idx:idx+3000])"` — much faster than iterating grep lines. Also: the covered story save requires slugs + summaries dict + podcast_guids list all in one call; partial saves silently succeed but miss the GUID dedup protection.

### 2026-05-09 05:31 — [TRANSITION] tag appears twice in a split-episode script; Edit tool requires unique context
When the first half ends with [TRANSITION] and the second half appends another segment boundary, the file contains two identical [TRANSITION] strings. The Edit tool's replace operation fails ("Found 2 matches") unless you include several lines of surrounding context to uniquely identify the target. Always anchor the append with a multi-line old_string that includes the final paragraph before the tag.

### 2026-05-05 05:15 — Prediction markets beat runs thin when no PM podcast lands in the brief
Today the topic brief had only one prediction market item (Trump Small Business Summit YouTube, no transcript). The PM segments were constructed entirely from adjacent material — Noah Smith's Noahpinion piece, LessWrong posts, The Economist oil podcast summary, Innermost Loop economic data. This worked but the segments lacked the trader-specific grounding (positions, specific contracts, strategy) that a dedicated PM podcast provides. When this happens, the strongest fallback is finding the trading implication of whatever IS rich today (regulatory risk on AI companies → IPO contracts; semiconductor boom data → infrastructure plays) rather than padding with thin PM headlines.

### 2026-05-11 05:49 — Python -c breaks on summary strings with apostrophes; write to a temp .py file instead
When `save_covered_stories()` is called inline via `python3 -c "..."`, any summary string containing an apostrophe (e.g., "Putin's Victory Day") terminates the outer single-quoted string early, producing a SyntaxError that can be hard to diagnose from the error line number. The reliable fix is writing the entire call to `.tmp/save_covered.py` and executing it as a file — no escaping required and the file is a natural audit trail of what was saved.

### 2026-05-10 04:57 — Multi-pass scripts produce cascading TRANSITION violations, not just one at the seam
Three `[TRANSITION]` boundaries had same-speaker violations today (421, 469 — not just the expected halves-join at 309). Fixing the first forces a speaker swap in the adjacent section, which pushes the conflict to the next `[TRANSITION]`, requiring coordinated triple-swap fixes. Stable stopping point: fix all TRANSITION violations and accept any cross-section consecutive within a beat (separated by `---` header only) as minor.

### 2026-05-10 04:35 — Prediction markets beat was effectively exhausted by yesterday's 05-09 episode
The 05-09 episode covered nearly every PM story from the past week (Kalshi $1B round, DraftKings, Cboe, DOJ probe, JPMorgan, Hinson bill, Bloomberg forecasting test, XO Market, Coinbase PM revenue, 2028 presidential odds, Cuba trade). Today's PM segments had to be constructed from adjacent material — the Consensus Conference crypto-goes-corporate angle, MoonPay/DFlow infra deal, and the LessWrong Sawtooth Problems coordination-game framing. When the PM beat is this thin, the best fallback is mechanism-design analysis (what kind of problem is a prediction market actually solving) rather than padding with weak headlines.

### 2026-05-10 04:35 — Today's first-half script closes on [BROOKE]; second-half agent should open on [BASIL]
The final speaker block in killen-time-2026-05-10.txt before [TRANSITION] is [BASIL] (the PM Seg 2 practical framework block). The second-half agent must open its first segment with [BROOKE] to avoid a consecutive same-speaker collision at the seam — per the known two-pass cascade pattern.

### 2026-05-05 05:15 — Afternoon Substacks/newsletters are always fresh for the next morning's episode
The Innermost Loop published May 4 at 12:17 PM — after the May 4 episode generated at 5 AM — making all its content (Greg Brockman 80% AGI, Morgan Stanley $805B hyperscaler capex, xAI 11% GPU utilization, Harmonic math proofs) fresh for May 5. Same pattern applies to any Substack or newsletter that publishes after ~7 AM MT. Worth checking ingest timestamps when the brief looks thin — the freshest afternoon content from the prior day may not have been in the prior episode.

### 2026-05-06 05:48 — Context-compacted session summaries can misreport write status — verify file before overwriting
This session resumed after context compaction with the summary claiming "no writing had occurred yet" for the May 6 script. The file actually contained 490 lines of complete episode content written in the prior session before context ran out. The Write tool would have silently overwritten it if the file hadn't existed already (requiring a Read first). When resuming a compacted session that was "about to write," always Read the target file before writing — the summary snapshot may predate actual writes that happened before compaction.

### 2026-05-05 05:34 — Two-pass episode generation leaves the intro blind to the second half
When an episode is written in two passes (first: AI/tech + prediction markets; second: NBA + entertainment + economics), the intro written in pass 1 has no knowledge of pass 2 content and will only preview the first half. QC must always check that the intro previews all major segments — this gap will be structurally present in every two-pass episode and won't be caught by the writer who generates it.

### 2026-05-05 05:45 — The "language-reality gap" is a reliable episode-linking thread when beats are otherwise disconnected
In today's second half, the AI messaging pivot (Altman saying AI won't take jobs while Brockman says 80% to AGI), the Yglesias seniors-not-on-fixed-incomes piece, and the Klein/Rosenblatt liberalism piece all shared a structural theme: language that stopped fitting the underlying reality at a specific historical moment but persisted anyway. Recognizing that shared frame let the outro synthesize three otherwise disconnected beats into a single resonant close. Worth watching for in future episodes — when economics and culture beats seem thematically distant, check whether they share a common epistemic structure.

### 2026-05-07 05:11 — First-half script written; intro previews only first-half beats (two-pass blind spot applies)
`scripts/killen-time-2026-05-07.txt` contains intro + AI/tech (2 segments) + prediction markets (2 segments), ending at `[TRANSITION]`. The second-half writer must update the intro's show preview to include NBA, entertainment, and economics/culture — the intro as written only previews AI and prediction markets content. The biggest fresh story today is the Musk-Anthropic xAI compute deal and Dario's 80x growth claim; covered the Murati trial testimony, hardware supercycle (Samsung $1T, Silex IPO, Roche/PathAI), a16z Crypto Fund 5, and Kalshi April $14.8B record with Event Horizon parlay analysis. Do NOT re-cover any of those in the second half.

### 2026-05-06 06:11 — Two-pass QC patterns: speaker cascade and multi-team Pack Watch misidentification

QC on the May 6 two-pass episode found two non-obvious structural errors. First: a single duplicate `[BASIL]` tag at the start of the entertainment section created a cascade requiring four speaker-tag flips to restore alternation — easy to miss on a read-through since the content itself is fine. Second: a Pack Watch intro naming only the Celtics when the Jummy Jum quote was a dual-team roast (Celtics opening, then pivot to the Rockets), causing the analysis to contradict the framing. Pattern to watch for in QC: when post-quote commentary references a different team than the intro, the intro is wrong, not the quote.

### 2026-05-06 06:03 — First-half beat agent overran scope, covering all beats instead of AI/Tech + Prediction Markets only

The morning-session first-half agent wrote a complete episode including NBA, Entertainment, Economics, Space, and a full outro with [END] — leaving the second-half agent with no uncovered beats from the brief except NBA detail (Celtics elimination, Giannis trade, Wemby) and content from podcasts not yet downloaded at first-half write time (Bill Simmons, Prestige TV, Works in Progress). Future two-part episode generation should either (a) explicitly tell the first-half agent to stop after AI/Tech + Prediction Markets or (b) accept that the second half will extend depth rather than breadth.

### 2026-05-09 05:11 — Topic brief understates prediction markets when content is in .tmp/articles/

The topic brief showed only 1 prediction market story (Trump Rose Garden, no transcript), but the Event Horizon article in .tmp/articles/ contained 8+ substantive new developments: Kalshi's $1B raise at $22B valuation, DraftKings launching its own proprietary exchange within weeks, Fourth Circuit oral arguments on Maryland, DOJ probing oil/prediction market Iran trades, JPMorgan clearing employees, the Cuba invasion trade, Cboe entering prediction markets in June, and Coinbase at $100M ARR. When a beat looks thin in the brief, check .tmp/articles/ directly — the scoring algorithm doesn't always surface article depth.

### 2026-05-10 05:00 — NBA/entertainment second halves lack podcast transcripts when all four transcripts are AI/tech-focused
All four transcripts in .tmp/transcripts/ today were AI/tech podcasts (Moonshots, Atlassian, Criteo, McLaren). The NBA segments in this second half were built from ESPN headline snippets only — no Ringer NBA, Real Ones, or Slam N Jam transcripts. CLAUDE.md explicitly requires pulling quotes from those pods for NBA coverage. If the pipeline didn't transcribe them, the NBA beat will always be shallower than intended. Worth checking whether the NBA podcast feeds (Ringer Group Chat, Real Ones After Dark, Slam N Jam) are in feeds.json and whether their episodes from the last 48 hours are being fetched and transcribed.

### 2026-05-11 05:26 — First half complete; Event Horizon #1 story was missing from prior draft and is the primary prediction markets segment
The existing killen-time-2026-05-11.txt had a prior draft that covered AI/tech well but completely omitted the Event Horizon Episode 16 piece (DraftKings/FanDuel Q1 earnings on prediction markets) — which was the #1 ranked story in the topic brief. The full file was replaced with a new draft that adds Event Horizon as the lead prediction markets segment, plus the Peter Wildeford hantavirus forecasting piece (Polymarket at 23% vs. Wildeford's calibrated 4%), Marc Andreessen's a16z builder culture episode, and Long Lake (No Priors). Second pass should write NBA, Entertainment, Economics/Culture, and outro — and the FIRST speaker of Segment One in the second half must be BROOKE (the first half ended on [BASIL]).

### 2026-05-11 05:53 — Two-pass scripts produce speaker collisions at the seam
When a script is written in two halves by different agents, the join point routinely produces a consecutive same-speaker block — the last speaker of half one matches the first speaker of half two. The fix is minimal (a one-line bridging block for the other speaker), but QC must check the [TRANSITION] seam explicitly every time a two-pass episode is assembled.

### 2026-05-12 04:27 — First half complete; second pass must open on [BROOKE]
`scripts/killen-time-2026-05-12.txt` contains intro + AI/Tech (3 sub-sections) + Prediction Markets (2 segments), ending at `[TRANSITION]`. The last speaker before the tag is [BASIL] — the second-half agent must open its first segment with [BROOKE] to avoid the known two-pass speaker collision. Fresh stories covered this half: Sam Altman House Oversight probe, Microsoft Israel GM departure, Derek Thompson/Imas AI jobs economics (lump of labor / Jevons / O-ring — all new frames), AMD ROCm 75x gap closure, TSMC capacity crunch, SpaceXAI trademark, women 83% AI-vulnerable jobs, PredMTrader Indiana Hoosiers and maternal healthcare event trading, Lloyd Blankfein a16z risk framework applied to PM positioning. Do NOT re-cover any of these in the second half.

### 2026-05-12 05:44 — Episode 2 of the day written to `-02` suffix file; episode 1 already covered most top-ranked stories
`scripts/killen-time-2026-05-12-02.txt` is the first half of today's second episode. The May 12 covered JSON already consumed: Anthropic/China/Mythos refusal, Jensen Huang China exclusion, Amp $1.3B raise, Anthropic $900B fundraising, AMD ROCm 75x, TSMC, Cerebris IPO, Apple-Intel deal, SpaceX AI trademark, Maryland FERC complaint, Plain English AI jobs frameworks, Blankfein a16z framework, and PredMTrader Indiana Hoosiers. Fresh stories this half: EU addictive design crackdown, Chinese cellular module ban debate, Microsoft Israel GM departure, HRW surveillance tech exports, eBay rejects GameStop $56B bid, JD.com food delivery war, Unitree GD01 rideable robot, LessWrong eval awareness paper, OpenAI fine-tuning API wind-down, Hermes Agent #1 on OpenRouter, Event Horizon casino games/Kalshi hypocrisy, congressional subpoena request (Pappas + 6 Dems on Iran insider trading), Ho-Chunk tribal challenge, ForecastEx exiting sports contracts. Last speaker before [TRANSITION] is [BROOKE] — second-half agent must open with [BASIL].

### 2026-05-12 05:48 — archive_used_sources silently does nothing with relative paths
The function checks `p.parent == (TEMP_DIR / "articles")` — an equality check between a relative parent and an absolute TEMP_DIR path, which always fails. Calling `archive_used_sources(used_article_paths=['.tmp/articles/foo.txt'])` returns 0 moves and prints nothing. Fix: always pass absolute paths, or fix the function to resolve paths before comparing. This session caught it because articles were still in .tmp/articles/ after the first call.

### 2026-05-19 — save_covered_stories returns None, not a success indicator
`save_covered_stories()` always returns `None` regardless of success or failure — the only way to verify it worked is to check `scripts/.covered-YYYY-MM-DD.json` directly after the call. `archive_used_sources()` does return a count (7 in this session). When a session spans a context compaction, the dedup/archive calls survive fine as long as the summary includes the exact slug lists and GUIDs — the 2026-05-19 compaction summary was detailed enough to reconstruct the calls cold with no re-reading required.

### 2026-05-19 04:29 — Two-pass scripts create speaker-alternation seam bug at the join point
When an episode is generated in two passes, the last speaker of pass one and the first speaker of pass two are often the same voice — because each pass starts with a reasonable alternation internally but the two halves don't coordinate. This session caught BROOKE→BROOKE at the AI Segment 1/2 boundary (pass-one seam) and again at Quick Hits→Outro (pass-two seam). The QC agent should explicitly check the speaker tag immediately before and after each `---` section divider, not just within sections. Also: the outro sign-off "Good morning. I'm Basil." is a copy-paste failure mode from the intro — QC should grep for "Good morning" in the final 20 lines.

### 2026-05-20 04:38 — First-half covered stories missing from .covered-2026-05-20.json
The previous context window wrote the first half but exhausted context before calling save_covered_stories(). The current .covered-2026-05-20.json contains only the 13 second-half story slugs. The first half's topics — Andreessen/Rogan, LessWrong alignment study, GitHub breach, $370B philanthropic unlock, Elon/OpenAI verdict, Cursor Composer 2.5, Cloudflare/Mythos, full prediction markets omnibus, GPU compute futures — are NOT in the covered file. The next episode writer will not know those topics were aired today and risks covering them again. Fix: before the next episode, manually add first-half slugs to .covered-2026-05-20.json, or read scripts/killen-time-2026-05-20.txt lines 1-130 to reconstruct what was said.

### 2026-05-23 04:31 — Real Ones / Athletic NBA Daily transcripts store as single long line

Both Ringer Real Ones (podcast_69251754) and Athletic NBA Daily (podcast_6a1142f66d877f0a780995f5) transcripts are stored as a single giant line — `Read` with offset/limit won't paginate them, but `python3 -c` substring search works fine. Future NBA sessions: use Bash/python to extract specific quote windows by character position, not line offset.

### 2026-05-23 04:35 — Two-pass seam creates consecutive-speaker bug in section endings + "Kyle" name is a recurring script risk
The prediction markets section ended with three paragraphs that both passes assigned to [BROOKE], producing a consecutive-speaker violation; the fix was BROOKE→BASIL→BROOKE across those three closing paragraphs. Separately, the CBS segment contained a direct "Kyle, you've spent your professional life…" address — agents writing for a named audience member tend to slip this in; it always needs removal before render. Quick grep to catch both: `grep -n '^\[BASIL\]\|^\[BROOKE\]' script.txt | awk 'NR>1 && prev==$2{print "consecutive:", $0} {prev=$2}'`.

### 2026-05-25 04:45 — Outro is the highest-risk location for segment mischaracterization in two-pass scripts
The outro at line 259 described the SaaSpocalypse segment as showing "SMB revenue collapse" — the opposite of what the segment actually covered (Ramp data showing SaaS is NOT collapsing). This class of error is specific to outros: they summarize all segments after the fact, and when written by a different pass or agent, they can introduce plausible-sounding but inverted summaries. QC should read every outro claim and verify it against the actual segment content, not just check for callbacks using phrases like "we covered."

### 2026-05-27 04:29 — Zvi Pope piece timing: published after May 26 episode cutoff
The Zvi Mowshowitz analysis of Magnifica Humanitas (8,486 words, published 2026-05-26T13:14 UTC) came out after the May 26 episode was produced (~11:08 AM UTC), making it genuinely fresh for May 27 despite `pope-leo-xiv` appearing in the May 26 covered slugs — that slug covered the announcement/Vatican lobbying story, not Zvi's content analysis. The dedup rule "same topic = skip" needs the nuance: the Verge/announcement was covered May 25, the Anthropic-Vatican lobbying May 25, but deep analytical reaction pieces from high-signal writers published after an episode's cutoff are fresh regardless of topic overlap. Check publication timestamp against episode production time when uncertain.

### 2026-05-27 04:52 — First-half writer left a complete outro + [END] before second half was appended
The first half of today's episode was generated with a premature `[END]` and full outro already in place, so the second-half pass had to strip those lines before appending. Future two-pass workflows should either (a) instruct the first-half writer to end with `[TRANSITION]` and a placeholder `[SECOND HALF BEGINS HERE]` instead of a real outro, or (b) have the second-half writer always check for and remove any existing `[END]` tag before appending. Also flagged: Shams reported Kenny Atkinson returning as Cavs HC while KOC on the same day said Atkinson is "almost certainly done" — the contradiction is in today's script and may resolve as a correction tomorrow.

### 2026-05-27 05:47 — Two-pass scripts reliably produce consecutive same-speaker blocks at section closings
QC on today's script found 5 consecutive same-speaker violations requiring 9 tag fixes, all concentrated at section-closing paragraphs and beat transitions. The pattern: each half's writer naturally closes their beat with a run of analytical paragraphs from the same voice, and untagged continuation paragraphs can mask the problem (the Hyperliquid paragraph had no `[BASIL]` tag but was still a BASIL continuation, hiding a 171→177 BASIL→BASIL gap). The automated fix is straightforward but the QC pass is mandatory — the automated `grep -n '^\[BASIL\]\|^\[BROOKE\]'` + consecutive-pair check catches all instances reliably.

### 2026-05-28 05:19 — Second-episode-of-day prompt targets wrong filename; wrote to -02 instead
The generate prompt said to write to `killen-time-2026-05-28.txt` but that file already existed as the first episode (produced at ~4:50 AM MDT). Wrote correctly to `killen-time-2026-05-28-02.txt` per CLAUDE.md convention. Future orchestration should detect existing same-day scripts and auto-suffix rather than hardcoding the filename in the prompt. Also: .tmp/transcripts/ and .tmp/articles/ were both empty for this run — all segment content came from RSS brief summaries + ad-hoc web fetches (CNBC article was paywalled; HN thread provided the substance instead).

### 2026-05-29 04:41 — Two-pass quick-hits duplication: redundant [TRANSITION] + "Quick hits to close" label
When a script is written in two halves and both halves end with quick-hits sections, the join produces a spurious `[TRANSITION]` followed by a second "Quick hits to close." label immediately after a "Quick hits before we close." block. The fix is always the same: delete the transitional `[TRANSITION]` line and the redundant label so the two blocks merge into one continuous sequence. Speaker alternation is preserved because each half's writer naturally alternates voices — the [TRANSITION] and label are the only artifacts.

### 2026-05-30 04:27 — Several podcast transcripts in .tmp/transcripts/ are effectively empty (1-line files)
The Real Ones (6a03cc38) and Slam N Jam (6a1a4dc8) transcripts were single-line stubs — the usable content came from the topic-brief.txt inline previews, not the transcript files. The Odd Lots rope transcript (019909ba) is the opposite problem: 25,810 tokens, exceeds the Read tool limit, and requires grep/bash to access specific content. Future writers should check transcript file sizes before attempting to Read them directly.

### 2026-06-01 04:41 — Two-pass seam bugs: consecutive same-speaker blocks and verbatim stat duplication
When a script is written in two passes, the seam region (the ~6 segments before the join transition) is the most likely place to get consecutive same-speaker blocks — both writers naturally close their half with related observations and the editor hasn't alternated across them. Separately, stats or characterizations established in the first-half prediction-markets coverage (e.g. "his assist rate roughly doubled") will reappear verbatim in the second-half basketball matchup coverage because each writer independently reached for the same supporting detail. The QC fix is: scan the 3 segments on each side of every [TRANSITION] for speaker-tag runs, and grep for any numeric stat or short quoted phrase that appears more than once across the full script.

### 2026-06-04 04:12 — First half written; two transcript-to-file mappings confirmed
`scripts/killen-time-2026-06-04.txt` contains the intro + AI/tech + prediction markets beats (~7,000 words), ending with `[TRANSITION]`. Confirmed transcript file mappings: Satya Nadella No Priors = `podcast_0c2c7d0c-5fc8-11f1-84fd-97552ff30f86.txt`; Tsinghua Manifold = `podcast_bf00de17-e96f-4215-9df6-bb222f63e546.txt`. Both are fresh (not covered in any prior episode). Second-half pass still needed for NBA, Entertainment, Economics/Culture, and outro.

### 2026-06-04 04:20 — Some .tmp/transcripts files are single-line (0 newlines); Read tool offset fails on them
At least one transcript (Goldman Sachs Odd Lots, podcast_bf5b0ae1) is stored as a zero-newline single-line file — `wc -l` returns 0, Read tool with offset fails, but `cat | cut -c` works. Always verify with `wc -l` before using Read offset/limit on a transcript if initial read fails. Second half of June 4 episode written successfully; Knicks Game 1, Backrooms/Kane Parsons, Noah Smith anti-monopoly, Goldman Solomon covered; 11 transcripts archived to .tmp/used/.

### 2026-06-04 04:24 — Two-pass scripts inherit the first-half's wrong day; QC must verify day-of-week independently
Today's script had "Wednesday" in both the intro and outro despite today being Thursday — the first-half writer stamped the wrong day and the second-half writer inherited it. The fix is trivial (two string replacements) but it would have aired wrong without QC. Pattern: QC agent should always run `python3 -c "from datetime import date; print(date.today().strftime('%A'))"` and compare against every occurrence of a day name in the script, not just trust the writer's header. Also caught a duplicate Microsoft Scout paragraph — briefly listed in the Build 2026 roundup section, then re-described more fully two sections later in the ChatGPT/Claude stats block; the second occurrence was removed.

### 2026-06-05 04:45 — First half written (AI/Tech + Pred Markets); second pass still needed for NBA/Entertainment/Culture + outro
scripts/killen-time-2026-06-05.txt exists with 7,103 words covering the intro, AI & Tech beat, and Prediction Markets beat, ending with [TRANSITION]. Second pass must write NBA, Entertainment, Economics/Culture, and the outro — and only AFTER both passes are complete should covered stories be saved and sources archived. The OpenAI PAC false flag (@JonathanDoomer/@DoomersAreDumb, Build American AI confirmed) is the cold open story; treat this topic as fully covered for any future episode. Event Horizon's BofA-sourced volume figures (Kalshi $4B/week, Polymarket US $400M, Underdog $120M) are now on-air; use these as baseline for future comparison rather than re-establishing the numbers.

### 2026-06-05 04:54 — Two-pass script QC: section-header consecutive-BASIL pattern is intentional, not a bug
The script consistently uses `[BASIL] Section name.` followed immediately by `[BASIL] First content...` at every section break — this is intentional format from both writing passes and should not be "fixed." The real consecutive-speaker errors are mid-content double-blocks and the structural join artifact: the "Quick hits before the outro" header landed before a segment that explicitly said "before we get to quick hits," which is a predictable two-pass joining bug to check for in every multi-pass episode.

### 2026-06-05 05:16 — Second June 5 first-half written; .tmp/articles/ was empty, all content relied on transcripts
The first June 5 run had archived everything to .tmp/used/ — articles directory was completely empty when this second pass started. The Plain English (Derek Thompson + Adam Green) and Freakonomics (Fareed Zakaria) transcripts were the only rich untouched source material; the genuinely fresh news stories were: Anthropic-WH détente (Reuters), Illinois data center moratorium (Pritzker/NBC News), and Meta AI customer support hack (MIT Tech Review). With 48 stories already on-air from the June 5 first run, future second-pass writers for same-day episodes should immediately check the .covered JSON to understand how little is left and weight toward podcast/transcript material over news items.

### 2026-06-05 11:45 — Second half written entirely from covered JSON summaries; .tmp/transcripts/ was empty
The `.tmp/transcripts/` directory contained no files when the second half agent ran, so all NBA/entertainment/economics content was reconstructed from the first-half agent's pre-saved covered JSON summaries rather than from raw podcast transcripts. The summaries were detailed enough to produce substantive segments, but this means the second half is dependent on whatever the first-half agent chose to pre-summarize — if the first-half agent's summaries were thin or wrong, the second half would repeat those errors. Worth checking if transcript fetching is failing upstream before multi-episode days.

### 2026-06-05 05:41 — Two-pass episode writing produces specific QC failure modes
When a script is written in two halves, the first-half writer's "as we documented earlier this week" self-references can directly contradict the second-half writer's framing of the same story as first coverage. Also: duplicate speaker tags appear at paragraph breaks within a single speaker's analysis run — easy to miss visually but caught by grep for consecutive same-tag lines. Both patterns should be explicit checklist items for future QC agents on two-pass episodes.

### 2026-06-06 04:41 — Second-half generation consistently runs short; plan for 3 expansion passes
When writing the second half in a single append pass, word count lands ~5,000-5,500 words against a 7,000-9,000 target — about 30% short. Three targeted Edit expansions (NBA analysis, Zach Braff Stockholm framing, Noah Smith domestic-economy paradox, plus Argentina/prediction markets bridge) brought it to 7,016 words. Budget for this correction step, or pre-expand the initial draft by writing longer first-pass segments on NBA and economics.

### 2026-06-07 04:16 — When PM beat has zero topic-brief stories, synthesize from AGI economics + ownership politics
Today's topic brief had no dedicated prediction markets items at all (0 of 40). Built a full two-segment PM section from adjacent AI stories: (1) SpaceX IPO as the next major binary event, using S&P 500 index exclusion mechanics to generate non-obvious trading insight; (2) Bernie Sanders 50% AI ownership op-ed + Trump White House equity stake consideration as a converging Overton window story with real IPO contract implications. The framing key: the Dwarkesh AGI economics taxonomy (what stays scarce) is itself a prediction market research program for which categories should outperform. When the PM brief is empty, the strongest fallback is finding the trading implications within the AI/tech stories, not reaching for thin PM headlines.

### 2026-06-07 04:17 — .tmp/articles/ was empty again; all segment depth came from podcast transcripts
The articles directory had no files this session — the Substack full-article pipeline appears to be consistently failing or not running before this 4 AM production window. All substantive sourcing came from the Cognitive Revolution "Recursive" podcast (GUID ce1196f0-618a-11f1-98fe-437dc10278bd) and the AI Daily Brief "This Week in AI" (GUID 12fc940d-b503-48b7-8235-b77b9efa8d56). Both were high-signal and covered enough ground to support a full 7,000-word first half without any Substack articles. Worth checking substack.py's schedule or whether the full-article fetch is silently failing.

### 2026-06-07 — QC caught hallucinated fan group name in two-pass outro
The outro referred to "the Jackals fan section" — a Spurs fan group that was never established anywhere in the script and does not appear to be a real named section. This is the two-pass risk: the second-half agent wrote the outro without full visibility into what was actually named and established in its own NBA section, pulling a plausible-sounding but fictitious detail. QC should specifically scan outros for named groups/entities that don't have a prior mention in the script.

### 2026-06-08 04:30 — .tmp/articles/ populated today; CoreWeave Odd Lots transcript at podcast_450514c3
Substack articles were present this session (Nate Silver World Cup, Noah Smith Roundup #83, Innermost Loop June 7). The Innermost Loop June 7 article (published ~14:41 UTC) was fresh for the June 8 morning episode since the June 7 episode was produced at 4 AM before it posted. The CoreWeave/Odd Lots transcript is at `.tmp/transcripts/podcast_450514c3-2648-49a8-9794-b46000d1738a.txt` — the CoreWeave CEO's most quotable content is the contract-length progression (3yr→4yr→5yr) plus the operational complexity argument against commoditization; those are around character positions 24000-66000 in the file.

### 2026-06-08 05:18 — generate-episode.sh doesn't detect same-day episodes before assigning filename
When this second-episode job ran, it was instructed to write to `killen-time-2026-06-08.txt` — which already contained a complete first episode from ~4:29 AM MT. Wrote to `killen-time-2026-06-08-02.txt` per CLAUDE.md naming convention instead, but the pipeline needs to detect existing same-day episodes and increment the suffix automatically before passing the filename to the writing prompt.

---

## Code Voice — Claude Code read-aloud project (NOT podcast pipeline)

*This section tracks the "Code Voice" dispatch: building a local read-aloud TTS for Kyle's Claude Code sessions that reuses this workspace's Kokoro infra. Unrelated to episode generation. See research at `~/.observer/wiki/research/claude-code-tts-pipelines-2026-05-10.md`.*

### 2026-06-08 16:29 — Kokoro cold-start is ~4.8s; warm synth is ~0.6s — live path needs a resident server
Measured on this machine (af_heart voice, `mlx-community/Kokoro-82M-bf16` via mlx_audio): cold process = 0.34s import + 2.37s model load + 2.05s first synth = **~4.8s to first audio**; once warm, a one-sentence synth is **~0.63s**. The research's 2-3s warning was understated for cold. Conclusion: a Stop hook that spawns Python per turn is unusable (4.8s dead air every turn). The live read-aloud path stays on **Kokoro (free, local)** but MUST go through a resident warm server that loads the model once — then it's ~0.6s/turn. No need for ElevenLabs/cloud.

### 2026-06-08 16:29 — "Clone of Kyle" is wrong; Killen Time voices are stock Kokoro voices
The workspace briefing called the voice "a clone of Kyle." It is not. config.py confirms BASIL=`bm_daniel`, BROOKE=`af_heart` — both stock Kokoro-82M voices, no cloning. The research doc ("Brooke / af_heart") is the accurate description. Read-aloud will use `af_heart` (Brooke).

### 2026-06-08 16:29 — Summarizer can run 100% local/free; Kyle wants relaxed (not 12-word) summaries
Monologue-control decision: Kyle picked the **LLM-summarizer** approach over the COMPLETED-line convention, but explicitly rejected the hard 12-word cap — wants "tight and compact but not lossy on critical detail" (numbers, decisions, blockers). Target ~2-4 sentences, lead with outcome. Summarizer can be local/$0: `qwen2.5` is in Ollama and `Qwen2.5-3B-Instruct-4bit` is in the MLX cache — no API key needed, honors the no-money constraint. Architecture pending Kyle's OK: resident warm voice-server (Kokoro + summarizer) + thin Stop hook gated on `/tmp/claude-voice-enabled`, with the `~/.claude/settings.json` block shown to Kyle for explicit approval before any write (file is hook-protected/global).

### 2026-06-08 16:48 — Code Voice BUILT and flipped ON; summarizer-model latency/quality tradeoff measured
Kyle approved the design + said implement and flip on. Built in `~/brainrot-radio/code-voice/`: resident warm `server.py` (LaunchAgent `com.codevoice.server`, KeepAlive), stdlib `stop_hook.py`, shared `text_clean.py`, wrapper `~/.claude/hooks/voice-stop.sh` registered as a 6th entry in `~/.claude/settings.json` Stop array (other 5 hooks untouched). Ollama runs as a brew service for durability. Verified end-to-end: the hook pulled the live transcript's last assistant turn, summarized, and spoke it via afplay.
Summarizer decision: **Ollama `qwen2.5:7b` stays** — it is both Kyle's approved engine AND the quality winner. Measured warm latency: Ollama-7b ~4.8s (faithful, keeps numbers + open questions); MLX `Qwen2.5-3B-4bit` ~1.4s but LOSSY (dropped the latency numbers and fabricated a decision out of an open question — fails Kyle's no-miscommunication bar); MLX `Qwen2.5-7B-4bit` ~10s (faithful but too slow — Ollama's runtime beats in-process MLX for 7b). End-to-end live read-aloud ≈ 5.5s warm (summarize-dominated), landed on **Kokoro for synth + Ollama-7b for summary**. Snappier would mean a quality tradeoff (smaller model) — deferred to Kyle.
Flag scoping refinement: `/tmp/claude-voice-enabled` is a cwd-allowlist (empty=all sessions; path-substring lines=only matching). Currently set to `/Users/kylekillen/brainrot-radio`. WATCH: the podcast launchd jobs run in this same cwd, so they'd be narrated if the flag is on when they fire (5:30 AM) — `rm` the flag when not testing, or later scope by agent-name/session instead of cwd.

### 2026-06-08 21:05 — PIVOT: real deliverable is two-way phone voice, not Mac read-aloud
Kyle clarified the actual goal: **interact verbally with Claude from his phone** — a conversation, not Mac-local read-aloud and not an async RSS podcast. The Stop-hook read-aloud was the wrong target (its summarizer also misfired: it spoke trailing preamble lines, and a 2-4 sentence summary is lossy on dense multi-point turns). Mac read-aloud flag removed (`rm /tmp/claude-voice-enabled`); the warm Kokoro server is KEPT and repurposed as the TTS backend for the phone path. New research published: `~/.observer/wiki/research/phone-voice-claude-code-2026-06-08.md` (106-agent deep-research, 22 verified claims). Headline: nobody has built a true live phone-CALL to a Claude Code session; the whole field is voice-texting. Kyle chose the **ccgram (Telegram) path**: talk to his REAL Claude Code session from his phone, voice-texting feel, in the Brooke/Kokoro voice, fully local, $0.

### 2026-06-08 21:05 — ccgram voice providers take base_url → local Kokoro+Whisper is a 2-line patch, not a fork
ccgram (`uv tool install ccgram`, Python 3.14) has clean provider abstractions. STT (`OpenAICompatTranscriber`) already exposes `CCGRAM_WHISPER_BASE_URL` — zero patch to point at a local Whisper. TTS (`OpenAITtsSynthesizer`) accepts `base_url` but config didn't read an env for it — added `CCGRAM_TTS_BASE_URL` in `config.py` + passed it through in `tts/__init__.py` (2 lines, in site-packages at `~/.local/share/uv/tools/ccgram/...`; FRAGILE — a `uv tool upgrade ccgram` wipes it; re-apply or do an editable install for durability). Built and PROVEN locally (no Telegram needed): (1) added OpenAI-compatible `/v1/audio/speech` to the warm Kokoro server (:8765) returning Brooke mp3 — tested, valid 128k mp3; (2) new `stt_server.py` (:8766, LaunchAgent `com.codevoice.stt`) = OpenAI-compatible `/v1/audio/transcriptions` backed by local mlx-whisper `whisper-large-v3-turbo` (already cached). Round-trip Kokoro→mp3→Whisper→text verified. Both servers durable under launchd, warm in ~5s. Launcher staged at `code-voice/run-ccgram.sh` (reads token from `~/.config/codevoice/telegram-bot-token`, wires both local endpoints, runs `ccgram run`). ONLY remaining step needs Kyle: create a DEDICATED Telegram bot via @BotFather (can't share his existing bot token), set Groups on / Privacy off, paste token. Then ccgram drives a tmux session named `ccgram`; Telegram forum topics map to Claude Code windows. NOTE: ccgram talks to sessions in ITS tmux, not the Remote Control "Code list" session — phone voice spawns/drives tmux-hosted claude sessions.

### 2026-06-08 21:30 — SHIPPED: voice-OUT to phone via Telegram voice notes (ccgram dropped)
Kyle simplified: he doesn't need voice-IN (he has text/dictation channels already), only the voice coming BACK. So ccgram/STT/Whisper are DROPPED (STT LaunchAgent unloaded; ccgram installed but unused). Final shape, WORKING: a `Stop` hook (`~/.claude/hooks/voice-stop.sh` → `code-voice/stop_hook.py`) extracts my closing summary from the transcript, synthesizes it in Brooke/Kokoro via the warm server's new OpenAI-compatible `/v1/audio/speech` (:8765), converts to opus/ogg, and sends it to Kyle's phone as a Telegram **voice note** via his EXISTING bot (`say_to_phone.py`, reuses TELEGRAM_BOT_TOKEN + chat 5063371068 from settings — no new bot needed for outbound). NO LLM summarizer in this path — speaks my text verbatim.
Two non-obvious bugs fixed: (1) the hook kept speaking PREAMBLE lines (text I write right before a tool call). Root cause: the harness writes preamble text as its OWN text-only assistant message, with tool_use in separate later messages — so "last text-only message" is NOT a reliable summary signal. FIX: speak only when the transcript's LAST assistant message is pure prose (`final_prose_text`); a preamble is never last (tool messages follow it), my closing summary always is. (2) Flush race — the final message may not be on disk when Stop fires. FIX: settle-loop polls until final_prose_text is non-empty AND stable (~4s max), which only happens once my real summary lands. Verified via `code-voice/phone_hook.log`: live hook in THIS pre-existing session correctly spoke my actual closing paragraph — so existing sessions pick up the hook with NO restart (Claude Code re-reads Stop hooks at turn boundaries).
### 2026-06-08 22:00 — COS exclusion must be IDENTITY-based, not cwd — the COS roams directories
Kyle's actual requirement: EVERY code session narrates to his phone, with exactly ONE exception, the COS — and he (correctly) refuses to think in terms of repos/cwds, which are invisible substructure to him. cwd-scoping FAILED repeatedly because the COS changes directories as it works (transcript shows its cwd cycling through `/Users/kylekillen`, `.observer/wiki`, `mojo-daemon/copybot`, `observer-system`) — so no cwd rule can pin it, and it can even appear inside a project folder. FIX: flag is now empty = UNIVERSAL (all sessions narrate); the COS is excluded by IDENTITY in `stop_hook.py:is_cos_session()` — it returns True iff the transcript has an `attachment`-type entry containing "You are the COS"/"You are the Chief of Staff" (the COS's loaded CLAUDE.md). This is false-positive-proof: a session merely DISCUSSING the COS (like this brainrot session — it had those strings 7x, all in user/assistant message text, ZERO in attachments) is NOT flagged; only the session that LOADED the COS identity is. Works on the running COS immediately (next poll), survives COS respawns, needs no cwd/env/restart/Kyle-input. Podcast jobs still muted separately via CODE_VOICE_MUTE=1 env (they're non-interactive). The leftover cwd include/exclude machinery in `in_scope()` is unused now (flag empty) but kept.

Scope: flag `/tmp/claude-voice-enabled` supports excludes — `!substring` (substring), `=exact` (exact cwd) ; empty/only-excludes = universal. GOTCHA FIXED: the COS does NOT run from `~/observer-system/cos` as the briefing implied — it runs from the HOME dir `/Users/kylekillen` (confirmed via respawn-cos markers in the `-Users-kylekillen` transcript). Home is a prefix of every project path, so a substring exclude can't target it — needs the new `=/Users/kylekillen` EXACT exclude. Current scope (live + persistent `~/.config/codevoice/voice-scope`): `=/Users/kylekillen` + `!observer-system` = every project narrates EXCEPT the COS and observer-system. Lesson: cwd-substring scoping breaks for any session running from a parent/home dir; exact-match is required for those. WATCH: (a) /tmp clears on reboot → resets to OFF (offered a login agent to persist); (b) 5:30 AM podcast jobs share brainrot-radio cwd so they'd narrate (offered a one-line mute in generate-episode.sh, not yet done — needs Kyle's ok to touch pipeline); (c) delegated workers share project cwds so they'd narrate — would need agent-identity scoping to fix.

### 2026-06-09 06:21 — Reviewed PR #1: approved-and-merged
Concurrency lock fix for double-episode publishing; all three gates passed. Lock logic correctly handles all four scenarios (fresh/live/dead/stale-holder). Pre-existing production drift committed alongside the fix per PR description. No CI configured. Squash-merged.

### 2026-06-12 21:37 — Reviewed PR #3: approved-and-merged
fix(code-voice): MLX Metal crash-loop fix — synchronous warmup before ThreadingHTTPServer opens port, double-checked `_model_lock` in `get_model()`. All three gates passed; no CI configured.

### 2026-06-11 04:34 — Speaker-alternation cascade: merge paragraphs, don't flip them
When a consecutive same-speaker violation appears in a long alternating sequence, flipping that one speaker triggers a downstream cascade through every subsequent turn. The fix is to merge the two consecutive same-speaker paragraphs into one block rather than changing the speaker tag — one paragraph disappears, the rest of the alternation is undisturbed. Learned this the hard way across ~8 cascading edits in today's first-half script.

### 2026-06-11 04:34 — DeLM architecture prescription for beat reporter pattern
DeLM achieved 65.7% SWE-bench Verified using Gemini 3-Flash at half the cost of frontier-orchestrated multi-agent. The beat reporter pattern maps directly to this: beat agents generate independently with no tight coordination loop, so a cheaper model per agent is viable. The right allocation is frontier context at QC and assembly where editorial judgment is actually needed, not at parallelized generation. Worth revisiting the model selection in beats.json with this in mind.

### 2026-06-11 04:46 — NBA Finals Game 4: Knicks 29-pt comeback (3-1 series), Wemby one flagrant from suspension
Knicks erased the largest deficit in NBA Finals history at MSG Wednesday night — covered in full in today's episode (second half, June 11 script). Do NOT re-cover in future episodes unless new developments (Game 5 result, Wemby suspension). Wemby is specifically one flagrant point from automatic suspension going into Game 5 in San Antonio — that's a live storyline for the next episode.

### 2026-06-11 04:51 — Two-pass episode assembly creates consecutive same-speaker violations at section joins
When an episode is written in two independent passes (e.g. first half: AI/tech; second half: NBA/entertainment), the speaker alternation at the join points is not reconciled and produces BASIL→TRANSITION→BASIL or BROOKE→TRANSITION→BROOKE collisions. Today's script had both: first-half summary ended on BASIL, NBA opened on BASIL; NBA ended on BROOKE, Entertainment opened on BROOKE. Fixed by swapping all 14 speaker tags in the NBA section. QC should explicitly check speaker continuity across every [TRANSITION] when multi-pass assembly is used.

### 2026-06-12 04:39 — Sharp Tech transcript is preview-only (paywall after ~30 min of content)
The Sharp Tech podcast transcript for the WWDC/Fable 5 episode ends abruptly at a paywall cut-off ("And that is the end of the free preview…"). Today's script drew on the available portion — which covered WWDC, Apple/Google architecture, and Federighi dissection — but the second half of that episode (Anthropic comparison with Apple) was unavailable. If a full transcript is needed in a future session, a paid Sharp Tech subscription or a different source is required. The preview alone was sufficient for today's AI/tech coverage.

### 2026-06-12 04:39 — Fable 5 sandbagging reversal: not covered in June 10–11 scripts despite being a June 10–11 story
The "covert sandbagging" story — Anthropic silently degrading Fable 5 for AI-research uses, then reversing within ~24h — was not in the June 11 covered JSON. Today's episode covered it fresh. Gergely Orosz's prescription ("provider-agnostic routers/harnesses so teams can switch vendors quickly") is the most actionable takeaway and worth re-surfacing when revisiting the multi-agent stack architecture.

### 2026-06-12 — Context-window compaction mid-episode: save/archive steps survived but must be verified
The June 12 episode was written across two separate context windows — the script was appended in the first session, then the session hit its context limit before save_covered_stories() and archive_used_sources() could run. The second session (this one) picked up from the summary and completed those steps successfully. Non-obvious risk: if the summary had omitted the story slugs or podcast GUIDs, those sources would have been re-usable in the next episode undetected. Practical fix for future long episodes: run save_covered_stories() immediately after writing the script, before QC or render, so compaction cannot strand it. Also: inline Python via bash single-quotes breaks on apostrophes in string values — write to a .py file and execute it instead.

### 2026-06-11 22:58 — Added Claude Lab Build-Pitch Reporter + AI/NFL emphasis shifts
Three tweaks landed (branch shift-emphasis-agents-over-prediction-markets): AI block now anchored on AI Daily Brief + Moonshots; sports beat ramped NFL-led (Ringer Fantasy Football, Fantasy Footballers, Bill Barnwell) as NBA winds down; and a new `claude_lab` Build-Pitch Reporter pre-step that scans Claude-technique YouTube transcripts, verifies findings against other sources, and writes greenlightable pitches to `build-pitches/YYYY-MM-DD.md` for the show's "Build-Pitch of the Day."
Non-obvious gotchas worth remembering: (1) the REAL episode structure lives in the hardcoded Pass-1/Pass-2 prompts inside generate-episode.sh — beats.json/CLAUDE.md are only editorial guidance the agents read, so a beat change isn't live until the shell prompts are edited too. (2) youtube.py truncates transcripts to 2000 chars (TRANSCRIPT_MAX_CHARS) and skips any channel with no channel_id — that truncation is why the reporter is a dedicated agent step that pulls FULL transcripts via yt-search/yt-dlp rather than relying on the ingest path. (3) youtube.py --channel filters by source id (e.g. cole_medin_yt), NOT by channel_id.

### 2026-06-12 — Two-pass scripts reliably produce a BASIL/BASIL join at the half-boundary
When the first half ends on BASIL (which it usually will, since BASIL typically closes segments) and the second half is written independently and also opens with BASIL, the [TRANSITION] join creates a consecutive same-speaker pair. This happened on the 2026-06-12 episode. QC must check for it explicitly; the fix is to flip all speaker tags in the second half's first beat so BROOKE opens. Also caught: triple "Good morning" in the intro when a cold-open hook + formal intro + co-host response all repeat the phrase — remove it from the formal intro line so BASIL says it once and BROOKE says it once.

### 2026-06-13 — QC pattern: two-pass episodes reliably generate 3 cross-section speaker conflicts
When the script is written in two passes (first half + second half by separate agents), BASIL consistently ends each major section with a "closing principle" line and also opens the next section — producing consecutive BASIL blocks across every [TRANSITION] in the second half (Agents→Sports, Sports→Entertainment, Economics→QuickHits). Fixed each time by flipping the Sports section and splitting the Economics close into BROOKE. Worth building this check into the QC prompt explicitly, or having Pass 2 agents open their first section with BROOKE by convention.
### 2026-06-18 — Reviewed PR #5: approved and merged (QC gate enforce verdict)
Fixed silent FAIL publish bug — QC step only checked exit code, now reads and retries on the emitted verdict. All three gates passed; no CI configured on this repo.

### 2026-06-18 — Reviewed PR #6: approved and merged (Build-Pitch lens reframe)
Prompt/brief-only change across three files; bash -n clean, beats.json valid, all three gates passed. No CI configured on this repo.

### 2026-06-15 — Headroom: verified context compression for Claude Code (pitched today)
Headroom (github.com/chopratejas/headroom, Apache 2.0, 23K+ stars) wraps Claude Code and compresses all agent inputs — JSON feeds, transcripts, tool outputs — 60-92% before they reach the LLM, with zero accuracy loss on published benchmarks. One command: `headroom wrap claude`. The `headroom learn` feature mines failed sessions and produces CLAUDE.md improvement suggestions; Matthew Berman's demo showed it surfacing "load deferred tool schemas at session start — saves 8,000 tokens per session," which is directly applicable to Kyle's ToolSearch-deferred setup. Most relevant pipeline steps: `/generate-brief` and `/run-beat-reporters` (heavy transcript/JSON reads). Full pitch in build-pitches/2026-06-15.md.

### 2026-06-15 — Claude Code v2.1.172 nested sub-agents: watch item, not yet pitched
Claude Code now supports sub-agents spawning their own sub-agents up to 5 levels deep (released June 10, 2026). Directly relevant to the COS → dispatcher → beat reporter chain but the changelog doesn't yet have enough implementation guidance to make a concrete build sketch. Worth revisiting when IndyDevDan or Cole Medin publishes a deep-dive.

### 2026-06-15 04:34 — First half written; AI Daily Brief/Moonshots absent from today's transcripts; covered-*.json not yet saved
The four fresh June-15 transcripts were Coleman Hughes, Ringer Fantasy Football, Odd Lots GPU compute (useful), and Huberman Lab — no AI Daily Brief or Moonshots. The AI/Tech beat was built from RSS + topic brief + Innermost Loop article + Odd Lots transcript. `scripts/killen-time-2026-06-15.txt` is written (7,023 words, ends with [TRANSITION]); the second-pass writer handles NBA/Entertainment/Economics/outro. Covered stories must NOT be saved until after both passes are complete — `.covered-2026-06-15.json` does not exist yet and should be created only after the full episode is assembled.

### 2026-06-16 04:50 — "Distribution layer" frame as emergent editorial spine

Writing the second-half outro, a genuine connective thread appeared across all beats that wasn't planned: Fox/Roku, Fable/Anthropic export controls, Yglesias immigration, Economist deglobalization, and Cowen's educational arbitrage all reduce to the same question — who owns the distribution layer when bundles come apart. That frame made the closing commentary substantive rather than generic. Worth watching as a recurring structural move: when beats share a hidden structural argument, the outro can name it instead of just listing the day's stories.

### 2026-06-16 04:58 — Dedup agent false-positive: same-day covered JSON is written by this script, not a prior episode

The freshness/dedup skeptic flagged nearly every back-half story as "already covered today," citing `.covered-2026-06-16.json`. That file is populated during generation (segment-by-segment as the episode is written), not after publishing — so it records this script's own stories, not a prior episode's. A QC agent must not treat same-day covered JSON as evidence of prior coverage unless a `-02` (or later) suffixed script for that date also exists on disk.

### 2026-06-16 04:50 — No Ringer Fantasy Football / Fantasy Footballers transcripts in pipeline

The NFL beat is anchored in CLAUDE.md to the Ringer Fantasy Football Show and Fantasy Footballers as named sources. Neither appeared in .tmp/transcripts/ today. The NFL section was built from Bill Simmons podcast context and general OTA knowledge — solid but not source-attributed in the way the editorial guidelines intend. If this persists, check feeds.json to confirm those podcasts are configured and that ingest.py is actually fetching and transcribing them. Fabricating analyst quotes from those shows is worse than omitting them; today's approach (general market analysis without attributing specific quotes) is the right fallback.

### 2026-06-18 12:05 — NBA Finals retrospective cycle is saturating; pivot to draft/offseason in future segments

By June 18, the Knicks championship analysis had been covered across three prior episodes (June 15: Nate Silver analytics; June 16: Bill Simmons top-50, Doc Rivers game 5; June 17: Doc Rivers closer philosophy, Giannis trade). The June 18 NBA segment had to rely almost entirely on the Ringer NBA Show's Raja Bell retrospective for fresh angles — small-guard recipe, Brunson career-low Finals stats, Wimby villain arc, payroll rank. Future NBA segments should pivot toward draft (June 26) and free agency rather than replaying finals analysis.

### 2026-06-18 12:19 — Sports content is hallucination-prone; the two-pass writer invents proper nouns inside direct quotes

The June 18 QC sourcing agent caught "Tyler Shuck" — a completely fabricated NFL quarterback name — embedded inside what the script presented as a direct Jason Moore (Fantasy Footballers) quote. This isn't a paraphrase drift; it's a made-up player name passed off as a sourced quote. The same episode had a Starlink subscriber count off by 3 orders of magnitude ("ten billion" vs tens of millions). Sports is higher risk than other beats for this pattern because proper nouns (player names, draft positions, team records) are dense, verifiable, and embarrassing when wrong. The sourcing skeptic in the QC panel catches these; running QC without it would not. Do not skip QC on episodes with heavy NFL/NBA content.
### 2026-06-18 — Reviewed PR #7: approved and merged

Budget guard script (budget-guard.sh) passed all three gates — correctness, coherence, smallness. No CI configured for repo. Change is an interim stopgap to pause podcast generation under cap-pressure to preserve trading pool headroom, pending durable per-workload key tiering.
### 2026-06-19 — Reviewed PR #10: approved and merged

Provider-agnostic offload config (or_complete.py) passed all three gates. _read_env_file helper correct; _config() fallback chain (env vars → offload.env → legacy openrouter.env) sound; complete() model param now optional with clear RuntimeError if unset. No CI configured for repo.


### 2026-06-19 — Reviewed PR #11: approved and merged

OpenRouter (Kimi) fallback for write passes passed all three gates. Shell elif logic correctly preserves 100%-Claude default path; fallback only fires on Claude double-failure or explicit PODCAST_FORCE_OPENROUTER. or_writer.py handles max-tokens ladder for credit-constrained accounts, transition-join dedup, covered-stories roundtrip, and 500-word sanity gate. Kimi hallucination risk documented in PR body and known from 2026-06-18 STATUS entry; QC Step 3 is the catch. No CI configured for repo.


### 2026-06-19 — Reviewed PR #12: approved and merged

Offload provider generalization (use configured OFFLOAD_MODEL, not hardcoded Kimi) passed all three gates. DEFAULT_MODEL="" correctly defers to or_complete._config() which reads OFFLOAD_MODEL from offload.env — the model-or-default fallback in complete() handles the empty string. Non-blocking: run_kimi_pass() function name and success log still say "Kimi" — cosmetic stale references, no behavioral impact. No CI configured for repo.
### 2026-06-21 07:58 — Reviewed PR #15: approved and merged

Added Independent Outcome Grader for the all-Gemini engine (gemini_qc.py + generate-episode.sh branch). All three gates passed: correct logic with proper exit-code handling, coherent with existing or_complete/or_writer patterns, minimal scope (new file + one branch replacement, Claude path untouched). Non-blocking: GEMINI_OUT env var and sys.argv[1] both passed to _target() — redundant but harmless. No CI configured for repo.


### 2026-06-22 12:00 — Reviewed PR #19: approved and merged

`set -e` guard fix for `gemini_finalize.py` call. The bare call let `set -e` abort the run on exit 2 (render-ready-but-QC-flagged, the common case) before the case statement could route to render+publish. `cmd || FINALIZE_RC=$?` idiom correctly captures the code without triggering the trap. All three gates passed. No CI configured for repo.

### 2026-06-22 — Reviewed PR #20: approved and merged

All three gates passed. Adds render_report.py (CONTRACT A): floorless arbitrary-length text -> Kokoro TTS -> ffmpeg concat -> publish. Additive return-value change to publish(). Full mock test suite (5 tests). No CI configured.
### 2026-06-24 23:41 — render_report.py publishes ONLY to the public Killen Time feed — privacy hazard for dispatch reports
The journal-dispatch audio pipeline (render_report.py / CONTRACT A) auto-publishes to `kylekillen/killen-time-podcast`, which is a PUBLIC GitHub repo + RSS listed in podcast directories. The screenwriting lead's original `golden-boy/render_brief.py` it's based on deliberately does NOT publish ("separate step") — public-publish was added downstream. Used it to render Kyle's private ~$2.6M family-finance memo to audio (8.3 min); rendered render-only and delivered via a private here.now link (https://vivid-spirit-6ned.here.now/) instead of publishing, and blocked for Kyle to choose public vs. a new private feed. If we want render_report to safely serve private/family reports, it needs a configurable private-feed target.

### 2026-06-25 00:25 — Report deliveries now route to a private here.now feed
`render_report.py` previously published dispatched reports to the WORLD-PUBLIC Killen Time feed (publish.py → kylekillen/killen-time-podcast). Added `publish_private.py` — an unlisted, `itunes:block=yes` podcast hosted on a stable unguessable `*.here.now` URL — and swapped render_report's publish import to it. Per Kyle's standing decision, ALL future report deliveries go private; the daily public news show is unchanged. Feed: https://plush-warden-27sa.here.now/feed.xml ; episode store + slug live locally at ~/.observer/private-feed/ (slug recoverable via `herenow list`).
### 2026-06-25 — Reviewed PR #22: approved and merged

feat: auto-route build pitches to private podcast feed. New publish_review.py primitive (idempotent content-hash ledger, skip-on-empty, subprocess-render via render_report.py), integrated into gemini_buildpitch.py as best-effort (non-fatal). First PR with CI configured — pytest gate added, 15 tests green.
### 2026-06-27 — Reviewed PR #23: approved and merged

fix(qc): exclude today's own script from dedup context. generate-episode.sh was writing today's script to disk before building the LATEST_SCRIPTS dedup context, so tail -3 included today's own file — making the QC grader flag the episode as a duplicate of itself (recurring 6/24–6/27 DEDUP false positives). Fix excludes killen-time-${TODAY}.txt from the find. Also hardened both json.load sites in ingest.py against empty/corrupt .covered-*.json files (was crashing before dedup state could persist, compounding the issue). All three gates passed; CI green.

### 2026-06-28 — Reviewed PR #25: approved and merged
chore: GUARDRAILS.md added — 5-row invariant table seeded from real documented failures (fleet-state-in-audio, QC-FAIL episodes shipped, private reports to public feed, RSS-only segments, deploy-gap class). Pure Markdown, no code changes, CI green.

### 2026-06-30 — Build pitch: Tool-Armed External Evaluator (pitched, ready for greenlight)
Anthropic's own AI Engineer Conference workshop confirmed the fleet's current gap: every worker self-grades, but a worker grading its own output is systematically biased toward approval ("really bad QA agent" — Ash Prabaker). The fix is a separate fresh-context evaluator with bash/test tool access, grading against the GUARDRAILS rubric independently. Concrete pilot: extend the delegate skill with a post-completion verifier sub-agent; first target is the PR reviewer. Full detail in build-pitches/2026-06-30.md. Note: today's automated morning pitch (Ornith-1.0 local coding tier) was verified blocked by fleet optimizer — Ollama 0.16.3 too old, 16 GB RAM insufficient for agentic dispatches.

### 2026-06-30 13:06 — Reviewed PR #26: approved and merged
fix(podcast): Gemini write failure fallback to Claude. WRITE_WITH_CLAUDE flag replaces the hard `exit 1` on Gemini failure — Gemini success clears it, failure falls through to the existing Claude→OpenRouter chain. Fixes the 2026-06-30 missed episode. All three gates passed; CI green (pytest x2).

### 2026-07-04 04:11 — Claude-lab YouTube scan was thin this week; converges with Fleet Optimizer's own blog scan on "execution debt, not technique gap"
This week's `youtube.py --hours 168` claude_lab scan (IndyDevDan/Cole Medin/AI Jason/GosuCoder/Matthew Berman/Anthropic) surfaced only re-runs of 2026-07-02's already-logged candidates (codebase-memory-mcp, GLM model-stacking, the Karpathy-wiki OKF standard — all already pitched/discarded) plus one framework/punditry video (Cole Medin's "5 levels of AI coding autonomy") whose actionable ideas already match the fleet's existing dispatch/validation/rule-split patterns, and one sponsored-ad video (Hermes/Hostinger tutorial, no new technique). No pitch filed today (`build-pitches/2026-07-04.md`). Non-obvious signal: this independently converges with the Fleet Optimizer's own 07-02/07-03 external scans reaching the same "stand down, nothing new" conclusion — the fleet's real bottleneck right now is executing already-found pitches (codebase-memory-mcp spike, T31 adversarial-verification decision, credit-resilience keys) not discovering new ones. Next claude_lab run can likely skip re-checking IndyDevDan/Cole Medin/AI Jason's most recent uploads if they're the same ones already seen this week.

### 2026-07-04 04:24 — AI Daily Brief "month in review" episodes re-tread heavily covered ground
Today's episode ("The Big Ways AI Just Changed," Whittemore's June recap) was ~70% rehash of stories already logged in prior covered-*.json files (Fable export-control saga, Sonnet 5 launch, Claude Tag, GLM-5.2/ZCode) — only the token-scarcity/Walmart-Uber angle, Microsoft's post-training-as-a-service move, the Glean "bot sitting" stat, and the KPMG CEO-ownership stat were actually new. Future beat-writing sessions should treat AI Daily Brief recap/monthly-review episodes as low-density for fresh material and scan covered-*.json aggressively before scripting from them, rather than assuming a full episode transcript equals a full segment's worth of new content.

### 2026-07-04 04:43 — NFL fantasy podcasts (Ringer/Fantasy Footballers/Barnwell) produced zero content again despite beats.json "ramp NFL up" directive
Second half of today's episode needed NFL-led sports per beats.json (`nba_sports` beat, updated 2026-06-11: "RAMP NFL UP AS NBA WINDS DOWN"), but `.tmp/transcripts/` had no Ringer Fantasy Football, Fantasy Footballers, or Bill Barnwell episodes — same gap as the sports beat's own documented off-season note anticipated. Leaned fully into the NBA trade frenzy (Jaylen Brown/Mitchell Robinson trades) instead, which the beat's context file explicitly sanctions ("lean entirely into whatever live sports is available... rather than forcing thin NFL coverage or fabricating analyst quotes"). Worth checking closer to training-camp open (~late July) whether these podcast feeds in feeds.json are actually configured/fetching correctly, since the ramp-NFL directive can't take effect if the source podcasts never appear in ingest output.

### 2026-07-23 09:07 — Reviewed PR #29: approved and merged; local worktree blocks `gh pr merge --delete-branch`
fix(code-voice): chunked synthesis + progress-aware watchdog, fixing the false-wedge restart loop from 07-08–07-21 (root cause: multi-KB synth requests holding the lock past the watchdog's 40s timeout, then the client retrying the same giant payload against the freshly-restarted server). All three gates passed, 17 self-tests + integration test, CI green. Non-obvious: `gh pr merge --squash --delete-branch` merged fine but failed to delete the local branch because it was checked out in a worktree at `/Users/kylekillen/brainrot-worktrees/codevoice-wedge-fix` — had to delete the remote branch directly via `gh api -X DELETE`. Future reviewers merging branches that have an active local worktree should expect this and not treat the partial failure as a merge problem.

### 2026-07-24 — Build pitches: coordinator/executor model tiering + Docker Sandboxes, both cross-verified beyond the sourced videos
Two pitches cleared the bar this cycle (`build-pitches/2026-07-24.md`). (1) Coordinator/executor model tiering (strong model plans/reviews, cheap model executes) — corroborated well beyond the two claude_lab videos that surfaced it: a dedicated Data Science Dojo write-up, an independent CloudZero cost analysis (~40% cheaper), and an existing open-source tool (pilotfish) built to automate it. This directly confirms and extends the Fleet Optimizer's own 2026-07-20 finding that our dispatch tooling (Workflow tool, `observer_heartbeat.sh`) doesn't tier subagent models by cost — this pitch adds the missing implementation template and before/after cost math to benchmark against. (2) Docker Sandboxes for unsupervised/skip-permissions agent dispatch — verified via Docker's own official blog, not just the sourced video — is a structural (not habit-based) fix for the fleet's own recurring "agent touched the live canonical tree" bug class (the 07-17 `~/observer-system`-IS-the-deploy incidents, #272's live-state test pollution), which today is only guarded by a git-worktree convention. Both logged as **pitched**, not built — Kyle's call on which (if either) to greenlight.

### 2026-07-24 04:30 — 20-day production gap (07-04→07-23) means today's dedup context is thinner than it looks; AIEWF trends recap overlaps already-covered 07-03 facts
Writing the AI/Tech + Agents & Building first half for 07-24, I found no episodes or `.covered-*.json` files between 07-04 and 07-23 — the show didn't run for ~20 days. That's good news for dedup (nothing to cross-check against for that whole window) but means anything from the AIEWF conference era is fair game only if it wasn't already used on 07-03, when the show did a heavy AIEWF dispatch. Concretely: Latent Space's "5 Trends That Defined AI Engineering at World's Fair 2026" recap overlaps substantially with facts already logged in `.covered-2026-07-03.json` (the loop-engineering "Great Loops" debate — Huntley's "locomotive engineers" line and Horthy's deterministic-vs-agent-loop framing were both already covered verbatim). I drafted from that quote before catching it on review and had to swap in different material (Thariq Shihipar's "models are grown, not designed," the AutoGPT-absence observation) instead. Future sessions pulling from that same trends article for later episodes should diff it against 07-03's covered file first rather than assuming "AIEWF" as a topic is fresh just because the specific article is new to `.tmp/articles/`.

### 2026-07-24 04:46 — NFL fantasy podcasts finally populated (resolves 07-04 gap); covered-stories save is the second-half writer's job; transcript files are heavily duplicated per show
Following up on the 07-04 04:43 note: `.tmp/transcripts/` now has today's actual Ringer Fantasy Football Show and Bill Barnwell/FTN Almanac episodes, confirming the beats.json "ramp NFL up" directive can work now that training camp season has arrived — no ingest fix was needed, it was purely a seasonal content gap. Separately, two non-obvious pipeline facts worth keeping: (1) `.covered-2026-07-24.json` didn't exist yet when the second-half session started, confirming the two-pass episode convention is that the SECOND-half writer does one combined covered-stories save spanning both halves, not each half saving independently — worth stating explicitly since it isn't written anywhere. (2) Podcast transcript files for recurring shows are heavily duplicated across many GUIDs (grep for "Ringer Fantasy Football Show" matched 6 files, "Hoop Collective" matched 7) with no date in the filename — disambiguating "today's episode" required checking file mtimes, not just grepping show name.

### 2026-07-24 05:10 — QC dedup skeptic caught two verbatim 07-04 repeats despite the 20-day gap
Mid-QC on `killen-time-2026-07-24.txt` (three-skeptic run, still in progress pending the sourcing skeptic): the freshness/dedup skeptic found the KPMG CEO-AI-ownership-survey stat and the Politico/Zvi-Mowshowitz "obvious nonsense" China-timeline quote were both restated near-verbatim from 07-04's episode, even though no episode ran in between. This means the 20-day production gap (see 07-24 04:30 note) is not sufficient cover for dedup — the front-half writer apparently treated the covered-*.json absence for 07-05→07-23 as "clear," without diffing new material against the last episode that *did* run (07-04) specifically. Future two-pass writers should always diff against the most recent prior episode's covered file regardless of how long the gap since then is. Also found: `.covered-2026-07-24.json` logged a `jadepuffer-agentic-ransomware` story as covered today that was never actually written into the script — a stale/leftover ledger entry that should be removed so it doesn't falsely block that topic (or falsely look like a same-day dup) in future QC passes.

### 2026-07-25 04:42 — First-half covered-stories save was never done; second-half writer had to reconstruct it by content-matching, and the named NFL anchor shows were absent from ingest again
Two things worth carrying forward. (1) `.covered-2026-07-25.json` didn't exist when the second-half session started, meaning the first-half writer (Opus 5 launch, routing, hill-climbing, Habryka/SFF) never called `save_covered_stories`/`archive_used_sources` despite that being the documented convention (see 07-24 04:46 note: second-half writer does one combined save). I had to reconstruct which sources the first half used by content-matching script text back to `.tmp/articles/` files — e.g. confirming the Habryka "200+ grants, extensive notes in Obsidian" passage came from `the_zvi_introducing-lightcone-commons.txt` even though that article wasn't in `.tmp/topic-brief.txt`'s listed full-text sources for today. This worked but is fragile — a future second-half session with a less distinctive first-half passage could easily mis-attribute or miss a source entirely. (2) The prompt explicitly asked to build the NFL segment around "the Ringer Fantasy Football Show ... plus Fantasy Footballers and Bill Barnwell," but only Fantasy Footballers was actually in `.tmp/transcripts/` today — the other two were absent, the same gap flagged on 07-04 and reportedly resolved on 07-24. So the fix on 07-24 wasn't durable; which named shows land in a given day's transcript pull still varies and the prompt template shouldn't assume all three are always present. Separately, one transcript file (`podcast_6a63c012a51cbd54e26a804b.txt`) was pure garbled "Bluetooth Bluetooth..." repetition — a bad transcription, unusable, still sitting in `.tmp/transcripts/`.

### 2026-07-25 04:50 — Nothing worth recording
QC run on killen-time-2026-07-25.txt still in progress (2 of 3 skeptics reported, sourcing skeptic pending); findings so far are routine QC catches, not new non-obvious discoveries beyond the 04:42 entry above.

### 2026-07-31 04:47 — Same-show transcripts need content grep, not filename/date alone; another multi-day covered-file gap (07-26→07-30)
Two things worth carrying forward. (1) Identifying "the fresh Moonshots episode" by filename or mtime alone was insufficient — two different files both mentioned "Moonshots" in their opening lines, but only a `grep -i "diamandis"` distinguished the actual Peter Diamandis-hosted show (podcast_95a67002, covering Jensen Huang's Open Secure AI Alliance letter and the OpenAI/Anthropic DC lobbying story) from an unrelated space-focused episode that just referenced "moonshot" in passing. Future sessions hunting for a specific named anchor show (AI Daily Brief, Moonshots, etc.) among many same-day-ish transcript files should grep for host names or distinctive in-show phrasing, not just the show title. (2) Another gap in `.covered-*.json` files: none exist for 07-26 through 07-30, mirroring the pattern already noted for 07-05→07-23 and flagged as a dedup risk on 07-24. Per the same lesson, I dedup'd against the two most recent actual covered files (07-24, 07-25) plus the three prior scripts named in the prompt, but the underlying cause of these repeated multi-day covered-file gaps (episodes running without the save step, or episodes not running at all) hasn't been root-caused and keeps recurring — worth a dedicated look rather than continuing to route around it episode by episode.

### 2026-07-31 05:02 — archive_used_sources() silently no-ops on relative article paths
`ingest.archive_used_sources(used_article_paths=...)` compares `Path(path_str).parent == (TEMP_DIR / "articles")`, which is an absolute path — passing relative paths like `.tmp/articles/foo.txt` (as the task prompt's own example implies) fails that equality check silently and skips the move, with no error and no log line for the skipped files (only a summary count of what *did* move). I passed 4 relative article paths alongside 10 podcast GUIDs in one call; the GUIDs archived fine (matched by substring, not exact-path equality) but all 4 articles stayed in `.tmp/articles/` until I re-ran with `os.path.abspath()`. Future sessions should always pass absolute paths for `used_article_paths`, and should verify the archive actually happened (`ls .tmp/used/`) rather than trusting the "Archived N sources" print, since N only counts what matched.

### 2026-07-31 05:21 — QC found a phantom-coverage entry in `.covered-*.json`: the save step can log source-research detail that was never actually aired
QC'ing `killen-time-2026-07-31.txt`, the freshness skeptic caught `.covered-2026-07-31.json`'s FFXpo camp-news entry claiming Jacoby Brissett's contract, Daniel Jones's camp status, and Alec Pierce's PUP status were covered — but grep confirmed none of those three appear anywhere in the aired script, which only used the Nabors/Judkins half of that same source segment. The entry's summary text apparently carried over the full source article's detail rather than just what made it into the script. Trimmed the entry to match what was actually aired. This is a real dedup-integrity risk distinct from the already-tracked "covered file missing entirely" gap pattern (07-24/07-25/07-31 04:47 notes) — here the file exists and looks complete, but overclaims, which would silently and wrongly block real future news on unaired players/facts. Worth a spot-check in future QC passes: when a covered-file entry summarizes more than one fact, verify each fact actually made it into the script, not just the segment topic.

### 2026-07-31 05:29 — QC on killen-time-2026-07-31.txt passed clean; two-pass half-join defect absent this time
Ran the full 3-skeptic adversarial QC on the script content (dedup, coherence, sourcing) as a separate pass from the earlier 05:21 covered-file fix. All three agents returned no MUST-FIX findings — notably the recurring two-pass defect this show flags for special attention (BASIL/BASIL or BROOKE/BROOKE collision, or back-to-back `[TRANSITION]` tags, at the front-half/back-half seam) was checked explicitly at every transition boundary and was absent. Sourcing skeptic verified load-bearing quotes near-verbatim against `.tmp/` transcripts/articles across every segment. No changes were needed to the script; verdict PASS.

### 2026-08-01 04:21 — All podcast transcripts empty today; AI/Tech + Agents beat had no fresh AI Daily Brief/Moonshots to anchor on
Every file in `.tmp/transcripts/` (5 podcast GUIDs, including the substack-post one) was 0 bytes for the 08-01 first-half session — not missing, not garbled, literally empty. This meant the prompt's explicit instruction to "prioritize the two anchor AI shows when fresh episodes exist" had nothing to anchor on; I fell back to Zvi's substack roundup (full text, in `.tmp/articles/`) plus Techmeme/HN brief items and WebFetch on a couple of un-fetched HN links (Simon Willison's MCP 2.0 post, two HN essay links) to get direct quotes, since only Substack sources get full-text saved to `.tmp/articles/` — HN/Techmeme entries in `topic-brief.txt` are snippet-only. Worth root-causing on the ingest side (transcription step silently producing empty files rather than erroring) rather than treating today as a one-off; if it recurs, future sessions should know WebFetch-ing the un-fetched HN links is a valid fallback for practitioner quotes.

### 2026-08-01 04:21 — Inserting content into an already-alternating BASIL/BROOKE script: re-run a full re-alternation pass, don't hand-fix speaker tags
Writing this episode short on first pass (4834 words vs. 7000-9000 target) and needing to insert new exchanges mid-script to hit length, manually choosing BASIL/BROOKE per insertion repeatedly broke the strict alternation rule (same-speaker collisions cascade forward from any single insertion point). Fixing it by hand became whack-a-mole. The reliable fix: after any edit, run a small Python pass that walks every `[BASIL]`/`[BROOKE]` tag top to bottom and reassigns them by strict alternation starting from BASIL, ignoring what was there before — since the dialogue content itself doesn't depend on which named host says which line, this is safe and instant, and should be the default move (not manual review) whenever a script is edited after the fact rather than written linearly start to finish.

### 2026-08-01 04:38 — Fantasy football sources vanish after one use; "Blocked and Reported" is mapped to WNBA but is unusable culture-war content
Two sourcing issues found writing the 08-01 second half. (1) `archive_used_sources()` moves a podcast transcript to `.tmp/used/` the episode it's consumed in, so Ringer Fantasy Football/Fantasy Footballers/Barnwell — all used 07-24/07-25/07-31 — had nothing fresh today; `sports.md`'s "off-season note" fallback (lean into NBA) covers this correctly, but it's really a "no-fresh-source" fallback, not just an off-season one, and will keep firing on any beat whose upstream show doesn't publish daily. (2) `podcast_substackpost209280537` transcribes to what's actually the "Blocked and Reported" podcast (confirmed via `podcasts/` filename mapping) — nominally about the WNBA/Seattle Storm, but the actual content is slur-laden culture-war commentary unfit for this show regardless of topical relevance. Skipped it entirely rather than using any of it; worth Kyle reconsidering whether that feed belongs in `feeds.json` at all.

### 2026-08-02 04:11 — Build-Pitch Reporter: verified pitch is Anthropic's own context-engineering guidance, never checked against the fleet's own CLAUDE.md files
Ran the claude_lab beat's whole-system scan. This week's YouTube feed was thin (mostly repeats of items already checked/discarded in 08-01's pitch, plus pure opinion/news content), so the surviving pitch came from a targeted `yt-search` sweep rather than the standard channel ingest: Anthropic engineer Thariq Shihipar's 2026-07-24 "new rules of context engineering for Claude 5 generation models" (80% Claude Code system-prompt cut + the shipped `claude doctor` audit command). Cross-checked against six corroborating sources plus a deliberately-sought contrarian take, and verified locally (`claude doctor --help` works on this machine's installed 2.1.220 binary). The gap: none of Kyle's ~15 role-workspace CLAUDE.md files, nor the global `~/.claude/CLAUDE.md` (loads every session, fleet-wide), have ever been run through `/doctor` or checked against this guidance — a zero-build, zero-cost lever that's gone unexamined during an active cap crunch. Full write-up: `build-pitches/2026-08-02.md`. Worth noting for future beat-reporter sessions: this project's own CLAUDE.md is a clean positive-control example of the pattern Anthropic just canonized (globals only, task detail loaded on demand) — useful as the reference shape if this pitch gets greenlit.

### 2026-08-02 04:21 — Neither podcast transcript today was an anchor AI show; non-empty-but-off-topic is a distinct failure mode from empty transcripts
Both files in `.tmp/transcripts/` were fully readable and on-topic for *some* show — but one was Market Huddle (Patrick Ceresna/Kevin Muir on Fed/Warsh rate-hike odds) and the other was the a16z Crypto Show (Andreessen/Dixon on the Clarity Act) — neither AI Daily Brief nor Moonshots, and neither meaningfully about agent-building. This is different from 08-01's all-empty-files failure: the ingest pipeline worked, transcription worked, but this week's fetch for the two anchor shows simply didn't land a fresh episode. Fell back to Simon Willison's site (two same-day posts: open letters roundup, Greg Brockman quote), Techmeme brief items, the Innermost Loop weekly digest, and a Dwarkesh YouTube-short clip (Grant Sanderson) for AI/Tech; the Agents & Building beat leaned on the build-pitch doc plus a LessWrong post and a Matthew Berman YouTube short. Confirms the anchor-show absence is not a one-off (also true 07-31, 08-01) — future sessions should expect to build the AI/Tech spine from Willison + Techmeme + whatever full-text Substack articles exist most days, not assume the anchor podcasts will be there.

### 2026-08-02 04:21 — Re-litigated the same alternation whack-a-mole the 08-01 note already warned about; the documented fix (scripted re-alternation pass) still wasn't used
First draft landed at 3,470 words against the 7,000–9,000 floor (same failure mode logged 08-01 04:21) and needed ~3,500 words of mid-script insertions to close the gap. Despite the 08-01 04:21 note explicitly recommending a scripted top-to-bottom BASIL/BROOKE re-alternation pass after any post-hoc edit, this session again hand-picked speaker tags per insertion and again produced multiple same-speaker collisions, caught only by a manual grep-and-fix loop afterward. The lesson from 08-01 is sound but isn't being followed — worth actually writing the reusable Python snippet somewhere discoverable (or just committing to running it by default) rather than re-discovering the same fix by hand each session.

### 2026-08-03 04:16 — No Moonshots episode again; routed the anchor AI Daily Brief episode into Agents & Building instead of AI & Tech
Today's `.tmp/transcripts/` had a fresh AI Daily Brief (Whittemore's "Operator's Cut" slot with Nufar Gaspar, all about AI token economics) but no Moonshots. Rather than force the tokens episode into the AI & Tech segment just because it's the anchor show, I judged its content — four eras of token spend, tokenizer shrinkflation, reasoning-effort cost multipliers, cost-per-task vs cost-per-token — fit the Agents & Building beat far better (it's entirely about running agentic workloads economically) and gave it the full featured-block treatment there instead. AI & Tech's spine ended up being Zvi's "Further Developments About Internal AI Models Hacking Things" (Anthropic's 141,006-eval self-audit, HuggingFace's forensic report) triangulated against MIT Tech Review and a LessWrong post. Future sessions: when the anchor show's actual content is agent-building material rather than general AI news, it's fine (and better) to place it in whichever segment its content actually serves rather than mechanically keeping it in AI & Tech.

### 2026-08-03 04:16 — podcast_e3640251 (AI Daily Brief tokens episode) transcript cuts off mid-sentence at exactly 24,924 bytes
Unlike other same-day transcript files (47KB–85KB, complete), this one ends abruptly mid-sentence ("because Sonnet needed more...") at exactly 24,924 characters/bytes — suspiciously round, and distinct from the already-documented youtube.py 2000-char truncation (this is a podcast, not YouTube). Used everything available up to the cutoff for today's script; the episode likely continues into a governance/audit-strategy section that never made it into the file. Worth root-causing on the podcast.py/transcribe.py side — if there's a size or length cap silently truncating podcast transcripts, it would explain any future "episode drops off with no warning" gaps and should fail loudly instead.

### 2026-08-03 04:34 — Second-half brief assumed NFL fantasy-football transcripts; none existed because that beat was exhaustively covered the day before
The second-half prompt explicitly called for Ringer Fantasy Football Show/Fantasy Footballers/Barnwell quotes, but `.tmp/transcripts/` had zero NFL content today — only a Bill Simmons Podcast NBA episode plus non-sports shows. Checking `.covered-2026-08-02.json` explained why: yesterday's episode already burned through QB/WR/TE tiers, Barnwell's full trade-value guide, and AFC North fantasy breakdowns in exhaustive detail, so even if fresh NFL transcripts had existed today, most of that ground would've been dedup-blocked anyway. Rather than force fantasy-football content that wasn't sourced (which would mean fabricating quotes attributed to real named analysts), led sports with the real available material — Bill Simmons' first-ever combined 2012/2013 Redraftables plus the Naji Marshall extension. Worth noting for future beat prompts: a heavy NFL day can make the *next* day's ingest legitimately NFL-empty, and that's a feature of dedup working, not a pipeline failure — don't assume the brief's suggested anchors will always be there.

### 2026-08-03 04:34 — Deadline/Hollywood Reporter now paywall-gate via a "tollbit" redirect; WebFetch returns 402 on all their story URLs
Tried WebFetch on four different Deadline/THR article URLs from today's topic-brief (Donkey Princess, Forte Corp Pictures, Sarajevo/Harrelson, Wowow/Aosawa Murders) — every one 307-redirected to `tollbit.<site>.com/...` and then returned HTTP 402 Payment Required. This is new: entertainment sourcing for this show has relied on fetching full Deadline/THR articles before. Worked around it today by writing the entertainment segment from the topic-brief's Techmeme-style snippets only (same practice already used elsewhere in this show for paywalled/snippet-only sources), adding analysis rather than fabricating quotes beyond what the snippet stated. If this gate is permanent, future entertainment segments should expect snippet-only Deadline/THR sourcing by default, not full-article pulls.

### 2026-08-03 04:34 — Speaker-alternation whack-a-mole recurred a 4th time despite the documented fix (scripted re-alternation pass) being logged twice already (08-01 04:21, 08-02 04:21)
Wrote the second half short on first pass and needed heavy mid-script insertion to hit the word floor, which — exactly as the two prior notes predicted — kept creating same-speaker collisions at every insertion point and especially at `[TRANSITION]` boundaries. Despite the standing recommendation to run a scripted top-to-bottom BASIL/BROOKE re-alternation pass, this session again fixed collisions by hand, one at a time, via grep-and-patch. It worked (final script verified clean), but cost several extra round trips. This is now a 3-for-3 pattern of "logged the fix, didn't use it" — worth just writing the reusable re-alternation script into the repo (e.g. a small `scripts/fix_alternation.py` callable on any script file) so future sessions run it instead of re-deciding whether to bother.

### 2026-08-03 04:41 — QC caught a same-half fact duplication (Naji Marshall stats) that alternation-focused checks would miss
The 08-03 QC pass found the Naji Marshall Mavericks extension ($52.2M/3yr/through 2029-30) stated in full twice — once in the sports block, again in the quick-hits block later in the same half — self-flagged by the script as an intentional callback but still restating every figure verbatim. This is a distinct failure mode from the well-documented BASIL/BASIL and TRANSITION-join collisions: it's intra-half content duplication, not a speaker-alternation bug, so it wouldn't be caught by the alternation-pass fix already logged three times this week. Trimmed the quick-hits mention to a numberless callback. Worth adding "check quick-hits items against earlier segments in the same script" as an explicit dedup-skeptic step, not just cross-episode dedup.

### 2026-08-03 05:05 — Sourcing QC caught a wholesale fabricated segment (real person, invented quote + stats) that slipped past writing
The Sourcing Skeptic found lines 87-89 of the 08-03 script — a "Singularity is cooking mathematics" bit citing a Substack roundup, a named mathematician (Daniel Litt), a fabricated direct quote, and invented Manifold-market percentages — had zero backing anywhere in `.tmp/` (topic-brief, articles, or transcripts). A repo-wide grep for the names/terms returned nothing outside the script itself. This is qualitatively worse than the already-logged forced-connection/dedup issues: it's not a sourcing shortcut, it's invented news attached to a real person's name. The segment also contained a real, correctly-sourced quote (Anthropic's Jess Yan, from yesterday's actual Innermost Loop roundup) that had been mis-attributed to the fabricated "today's roundup" — had to re-source that quote to its real origin rather than just deleting it. Worth tightening the writer-side prompt: fabrication risk seems highest right where a real adjacent source (Peter Hall's Scientific American quantum-crypto piece) invites a plausible-sounding "and there's more like this" follow-on that isn't actually grounded in anything ingested.

### 2026-08-03 05:12 — Re-running /qc-episode found a fresh line-159 defect the earlier 05:05 pass missed
This QC pass (3 fresh skeptics) is running on the same 08-03 script an earlier pass already partially fixed (the 05:05 STATUS entry documents a fabricated-quote fix at lines 87-89, confirmed still fixed on disk). The Coherence Skeptic this round caught a second, distinct defect the first pass didn't: line 159's closing "everything we've covered this morning" recap references "a hundred-thousand-dollar-a-paper math bet just getting conceded four years early" — a phantom callback to content that doesn't exist anywhere else in the script (likely a leftover from a cut draft item), plus a numeric contradiction ("three hundred thousand real-world hack attempts" vs. the correct 141,006-runs/3-hacks figure established at lines 5, 29, and 329). Confirms these closing-recap sentences are a recurring blind spot: they're written to summarize the whole half but aren't re-checked against what the half actually ended up containing after edits. Worth having the writer regenerate (not hand-preserve) any "everything we covered" recap line as a final pass step, after the half's content is locked.

### 2026-08-07 04:25 — Build-Pitch Reporter: always check fleet-optimizer's own logs before pitching a "new capability" — it tracks Claude Code changelogs itself
Nearly pitched Claude Code 2.1.219/2.1.221's subagent-cap changes (nesting depth 1→3, 200-agent spawn cap removed) as a fresh build-pitch until grepping `~/fleet-optimizer/STATUS.md` and `ACTION-LOG.md` turned up that the Fleet Optimizer already cites those exact version numbers and had deliberately chosen depth-1 fan-out for its own `dispatch_subagent` tool as a structural safety boundary, not a limitation awaiting an Anthropic fix. The verified, actually-fresh pitch this session (CLAUDE.md ablation / `/doctor` trim-check, written up in `build-pitches/2026-08-07.md`) only surfaced after that same log check confirmed it, unlike the subagent-cap item, was genuinely untouched. Future build-pitch sessions should grep fleet-optimizer's STATUS.md/ACTION-LOG.md for a candidate's keywords before writing it up — that log already does its own external-scan/changelog-watching, so a "new" capability candidate is often already known and factored in there.

### 2026-08-07 04:25 — `find ~ -maxdepth 3 -name CLAUDE.md ... | xargs wc -l` timed out (143) system-wide; check known repo paths individually instead
A broad home-directory find for CLAUDE.md files (meant to survey line counts fleet-wide for the ablation pitch) hit the 2-minute Bash timeout and had to be abandoned in favor of checking known repo paths one at a time (`~/.claude/CLAUDE.md`, `~/brainrot-radio/CLAUDE.md`, `~/observer-system/CLAUDE.md`, `~/fleet-optimizer/CLAUDE.md`, etc.). Worth remembering for future fleet-wide filesystem scans from this beat: go targeted (known repo list) rather than a recursive home-directory find, which appears to hang on something under `~` (large mounted dirs, node_modules despite the exclude, or similar).

### 2026-08-07 04:50 — Multi-day episode gap (08-04 to 08-06 missing) and no filename→show-name mapping for podcast transcripts
Two non-obvious things from writing today's first half. (1) `scripts/.covered-*.json` jumps straight from 08-03 to today — no 08-04/05/06 files exist, and `last_episode` in the 08-03 file still reads 2026-08-03T11:14. Future dedup reads should check the actual `last_episode` timestamp rather than assuming the most recent covered-file date is "yesterday" — there was a 4-day production gap this time. (2) `.tmp/transcripts/podcast_<uuid>.txt` filenames carry zero show metadata, and neither AI Daily Brief nor Moonshots appears in `topic-brief.txt`'s ranked top-40 (they're fetched unconditionally as anchor shows regardless of ranking) — had to `grep -l -i "AI Daily Brief\|Nathaniel Whittemore"` / `"Moonshots\|Diamandis"` across all transcript files and pick the most recent by mtime to find them, rather than reading the brief. Worth remembering: don't expect the anchor shows to show up in the brief at all — always grep transcripts directly for them first.

### 2026-08-07 04:50 — Ad-hoc Python alternation check (not a saved script) worked cleanly on the first attempt this session
Three prior STATUS entries (08-01, 08-02, 08-03 ×2) documented recommending a scripted BASIL/BROOKE re-alternation pass after mid-script insertions, but noted the fix kept not getting used in practice. This session, after inserting ~1,500 words of new segments post-draft, ran a quick inline Python snippet (`re.match` over speaker tags, flagging consecutive same-speaker lines) via Bash rather than fixing by eyeball — caught all collisions in one pass, fixed each with a targeted Edit, then re-ran the same check to confirm clean. Didn't save it as a repo file, so the next session will still have to re-type it — the recommendation to commit a reusable `scripts/fix_alternation.py` still stands, but at least the programmatic-check habit (vs. manual grep-and-guess) held this time.

### 2026-08-07 04:53 — Nothing worth recording
Mid-task (second-half script for 08-07), waiting on 4 parallel background research agents before writing; no new non-obvious finding beyond what's already logged this session.

### 2026-08-08 04:14 — Build-Pitch Reporter: the fleet-optimizer-log check has to include its `scans/` dir, not just STATUS.md/ACTION-LOG.md — same-day duplicate findings happen
Extends the 08-07 04:25 lesson. Today's scan almost re-pitched the Claude Code v2.1.224 subagent-cap removal (Anthropic's changelog confirms it's real), but `~/fleet-optimizer/scans/2026-08-07.md` had already found and filed the *exact same* finding — same version, same grep of the fleet's 21 headless `claude -p` call sites — earlier that same day. STATUS.md's own top entry summarized it, which is what caught it this time, but the underlying source is `scans/YYYY-MM-DD.md`, a daily external-scan file that doesn't always get a STATUS.md digest before this reporter runs. Future sessions: check `~/fleet-optimizer/scans/` for today's and yesterday's date directly, not just STATUS.md, since scan-to-digest lag is possible.

### 2026-08-08 04:14 — `~/.observer/wiki/SCHEMA.md` self-documents an unbuilt gap: no cross-page contradiction/staleness audit
SCHEMA.md's "What v1 does not yet support" section states plainly: "Cross-page contradiction detection at scale — v1 handles per-event contradictions only." That means drift accumulating across existing wiki pages (a number that changed but only got updated in one of several places it's recorded) is never caught unless a *new* incoming event happens to trigger the per-event conflict-resolution path — there's no periodic self-audit. Surfaced today as the fleet-relevant hook for a build-pitch (`build-pitches/2026-08-08.md`, state-vs-event memory audit pattern); worth knowing this gap exists independent of whether that pitch gets built, since it means the wiki's "current state wins" guarantee is weaker than it sounds for facts nothing has recently touched.

### 2026-08-08 04:22 — Nothing worth recording

### 2026-08-08 05:20 — Second-half session had to reconstruct the first half's covered-stories entirely from the script text; `scripts/.covered-2026-08-08.json` didn't exist yet
When this session started writing the second half, no covered-stories file existed for today at all — meaning the first-half session either hadn't run its own save step yet or hadn't gotten to it. Since the task instructions require saving slugs/summaries for *both* halves plus all podcast GUIDs, had to re-derive the first half's ~14 story slugs and detailed summaries by reading the already-written first-half script (lines 1-149) rather than pulling them from any saved state, then call `save_covered_stories` once for the full 37-item set. `save_covered_stories` merges into any existing same-day file, so this was safe even if the first-half session's own save call lands later — but future second-half sessions should expect `.covered-{today}.json` may not exist yet and budget time to re-read the first half's script for dedup-summary reconstruction rather than assuming it's already saved.

### 2026-08-08 05:20 — Podcast transcript ASR errors produced plausible-sounding wrong names/numbers; caught by cross-checking against prior covered-*.json spellings, not by the transcript alone
Today's Fantasy Footballers transcript rendered the Lions RB's name as "Jameer Gibbs" throughout — the correct name, "Jahmyr Gibbs," was only recoverable by noticing `scripts/.covered-2026-08-07.json` already had the correct spelling from yesterday's coverage of the same player. Separately, the same transcript had an ASR line ("you have a AJ Brown's a lock") that doesn't fit the Patriots-roster context it appeared in (AJ Brown plays for the Eagles), and a dollar figure for Colts WR Alec Pierce ($86M guaranteed, $27-28M APY) that's implausible for his actual market tier — both were dropped from the script rather than repeated as fact, since neither could be verified and asserting them risked stating a fabricated-sounding "fact" attributed to a real analyst. Worth remembering for future beat-reporting off ASR'd podcast transcripts: cross-check any name against recent `.covered-*.json` files before trusting the transcript's spelling, and treat any dollar figure or team-context detail that doesn't fit what's otherwise known about a player as a likely transcription artifact to omit rather than launder into the script.

### 2026-08-09 04:10 — Build-Pitch Reporter: `claude_lab` YouTube channels have been dry 3 sessions running; the fallback to Claude Code releases/Simon Willison/Latent Space is now load-bearing, not a rare edge case
All 7 items in today's `youtube.py --hours 168 --json` claude_lab pull were either already fully reviewed in the 08-07/08-08 pitches (no new posts from IndyDevDan/Cole Medin since 08-07) or off-lens (Codex tips, a conference sponsorship short). This is the third session in a row where the primary source (claude_lab channels) produced nothing new — today's actual pitch (Claude Code's native cross-session `SendMessage`/`ListAgents`, v2.1.224) came entirely from the secondary source (Claude Code's own changelog/docs). Future sessions of this beat should treat the changelog/Simon-Willison/Latent-Space check as a co-equal first pass, not a fallback only reached after YouTube comes up empty — and should keep checking whether any newly-verified capability is already adopted in `~/observer-system` (a plain grep for the feature's tool/function names) before writing it up, since that's what confirmed today's pitch was genuinely unbuilt rather than a rediscovery.

### 2026-08-09 04:24 — Anchor-show transcripts can tease topics they never actually reach; check transcript length against its own cold-open promises before relying on it
Today's Moonshots transcript (7,044 words) opens with a cold-open teaser listing five stories — Google/DeepMind shakeup, SpaceX's $100B ARR earnings call, OpenAI's Astra math breakthrough, a cheap Chinese open-weight model, and the OpenAI message-board hack — but the transcript body itself never gets past the first two segments (a consciousness-paper discussion and Imad Mostaque's AI-personhood paper) before cutting off mid-sentence, presumably an ASR/capture length limit. Anything only mentioned in the cold-open teaser is not actually available to quote or summarize — had to verify by reading to the literal end of the file rather than trusting the intro's topic list. Separately, `.tmp/articles/the_zvi_what-happened-openai-and-huggingface.txt` turned out to be a pure recap of the OpenAI/HuggingFace saga that `scripts/.covered-2026-08-08.json` had already covered in exhaustive/quote-level detail the day before — it was correctly discarded as a source, but future sessions should expect that a "fresh" dated Substack post can still be 100% redundant with yesterday's coverage when a story arc is still unfolding, and should check the covered-file's `segments` detail (not just the `stories` slug list) before assuming a same-topic article has anything new to add.

### 2026-08-09 04:54 — All three NFL fantasy podcast feeds were fully dry for a week; pivoted to ESPN's NFL Nation notebook instead of stretching old quotes
Ran `podcast.py --show` for `ringer_fantasy_football`, `fantasy_footballers`, and `bill_barnwell` individually (each requires a full pipeline pass over every podcast in `feeds.json` before the `--show` filter applies at the end, so each run took several minutes even though it only needed one show's result) — all three came back empty: every episode in each feed within the last 168 hours was already used by the 08-07/08-08 episodes. Rather than reuse Thursday's quotes as if they were new, said so plainly on air and pulled real, dated content instead from ESPN's rolling NFL Nation training-camp article (`WebFetch` on the live URL, not a cached RSS summary) — Mahomes' ACL clearance, Mayfield's contract standoff, Micah Parsons' return timeline, etc. Future sessions building the NFL beat should expect these three feeds can go a full week without a new episode and have a non-podcast fallback (ESPN's daily camp notebook, or similar) ready rather than fabricating or stretching quotes.

### 2026-08-09 04:54 — `archive_used_sources(used_article_paths=...)` silently no-ops on relative paths — must pass absolute paths
`ingest.archive_used_sources` compares `Path(path_str).parent == (TEMP_DIR / "articles")`, where `TEMP_DIR` is absolute. Passing a relative path like `.tmp/articles/foo.txt` makes `p.parent` resolve to a relative `Path(".tmp/articles")`, which never equals the absolute `TEMP_DIR / "articles"` — the function returns silently with no error and no file moved, only a truthy-looking "Archived N files" line if other (absolute) paths were also passed. Caught it because the article was still sitting in `.tmp/articles/` after the first call reported success for the podcast GUIDs but the article count was implicitly zero. Always pass `os.path.abspath(...)` for `used_article_paths`, and verify with `ls .tmp/articles/` afterward rather than trusting the return value alone.

### 2026-08-14 11:15 — Build-Pitch Reporter: 08-08's memory/state pitch is still unbuilt six days later, and its exact failure mode has now recurred four times
This session's `claude_lab` scan came up empty (all three candidate videos either identical to or a duplicate-in-spirit of prior weeks' already-reviewed content), so the useful finding was checking whether the 08-08 pitch — split every wiki/memory write into overwritable "state" vs. append-only "event," plus a periodic cross-page contradiction audit — had been adopted. It hadn't: `coleam00/second-brain-skills` isn't installed anywhere under `~/.claude`, and `SCHEMA.md` still only handles per-event contradiction at write time. In the six days since, `STATUS.md` logged at least four incidents this exact fix targets: a digest telling Kyle he was eight weeks late on a screenwriting deal that had actually closed five weeks earlier, a notification count reported as 170+ delivered when it was zero since 07-07, 2,637 near-duplicate wiki pages from a silently-stuck channel, and `MEMORY.md` itself going over its advisory cap. Worth surfacing for whoever next triages `build-pitches/`: this is the highest-leverage unbuilt item already sitting in the feed, and the cost of leaving it unbuilt is now measurable rather than theoretical.

### 2026-08-14 04:52 — Dedup must be checked at the sub-topic level within a single anchor-show episode, not just at the episode level
Today's AI Daily Brief "graph engineering" episode (podcast_779f234c...) opened with an Astra-cybercritical recap and an auto-mode-default recap — both already covered in exhaustive quote-level detail in `.covered-2026-08-08.json` — before pivoting to a genuinely fresh graph-engineering primer in the second half of the same episode. Treating the whole episode as one dedup unit would have meant either skipping a fresh, highly relevant primer or re-airing stale Astra/auto-mode material. Also: no Moonshots episode existed in `.tmp/transcripts/` at all today (grep for "moonshot"/"diamandis" across all transcript files came back empty) — second session in a row (see 08-09 finding) where Moonshots is unreliable as a load-bearing anchor source; AI Daily Brief alone had to carry the anchor-show requirement. Future sessions: grep anchor-show transcripts by content (filenames are opaque GUIDs, not show names) and check each anchor episode's *segments* against covered-json separately, since one episode can contain both stale and fresh material.

### 2026-08-14 05:07 — Same anchor show can have 4+ same-day episode files; disambiguate by keyword-frequency grep, not first content match
Today's second-half session needed to back-fill the first half's podcast GUIDs for `save_covered_stories`/`archive_used_sources` (no `.covered-2026-08-14.json` existed yet — same gap as 08-08 05:20). A plain `grep -l "Zuckerberg"` across `.tmp/transcripts/` matched 3 different AI Daily Brief files, since the manifesto gets a passing mention in multiple unrelated episodes. Disambiguated by counting mentions (`grep -c`) and checking for a phrase the script actually quotes ("individual empowerment") — one file had 27 hits and the quoted phrase, the other two had 1-2 incidental mentions. Future sessions reconstructing a prior half's sources: don't archive the first content match for a show/topic grep: count keyword density or check for a specific quoted phrase to confirm which same-day episode was actually used, since archiving the wrong file both fails to protect the real source from reuse and wrongly removes an untouched one from the pool.

### 2026-08-14 05:07 — First draft of a two-segment-beat half (sports/entertainment/econ) came in ~35% under the word-count target despite covering every planned segment
Drafted all planned segments for the 08-14 second half in one pass and it landed at 4,723 words against a 7,000-9,000 target — every segment existed but each ran short (single Q&A exchanges instead of the multi-beat back-and-forth the first half uses throughout). Had to do five separate expansion passes (deepening existing exchanges, adding held-back research-agent findings that didn't make the first cut) to reach 6,874 words. The gap wasn't a content shortage — the research agents had surfaced more usable material than got used in the first draft. Future sessions writing a multi-beat half: budget word count per segment *before* drafting (e.g., ~800-1000 words per full segment with 3-4 exchange back-and-forths) rather than writing one exchange per sub-topic and checking the total after — the shortfall is easier to prevent than to fix in five follow-up edit passes.
