### 2026-09-25 05:20 — yt-dlp transcript 429 has a partial workaround: `--dump-json` description still works
Subtitle downloads returned HTTP 429 all week, but `yt-dlp --skip-download --dump-json <url>` still returned the video description and chapter list, which was enough to triage Cole Medin's rate-limit video (planner/executor split with GLM 5.3 Flash, already pitched/piloted as model-router #989). Description-only reads are triage signal, not verification — the pitch file says so explicitly. Also: GLM 5.3 Flash pricing disagrees between OpenRouter ($0.045/$0.14) and our wiki ($0.075/$0.25); re-check before budgeting.
