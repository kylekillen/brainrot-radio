### 2026-09-25 11:35 — 09-25 miss: the run did not die silently, it aborted at the word floor and nothing raised; three guards added
Corrected diagnosis: the 05:15 recovery run logged "Voice render failed, aborting" at 05:54 (generate-20260925-051500.log) because QC's rewrite left 4,948 words against the 6,000 render floor — the guard worked; the run's own log just never reached a human, and `recovery-*.log` only held the burn banner because `log()` wrote to files, not stdout. Shipped in one PR: (1) writer prompts carry `.claude/context/evidence-rules.md` + a per-item evidence ledger + a 7-day AIRED digest, `dedup_guard.py` checks the first half before the back half is written and hands its findings to QC, and the prompt now inlines up to 16 real transcripts (it was the first 3 alphabetically, so most "(transcript available)" items were blurb-only in practice); (2) the covered ledger is deferred (`COVERED_DEFER=1`) and written by `covered_guard.py` from the FINAL script after publish; (3) `run_guard.sh` EXIT trap + `watch-episode.sh` (launchd, needs `install-watch.sh` after merge) alarm through `raise-alarm.sh --no-telegram`. Not done, on purpose: no top-up loop for a QC-shortened script (Gemini engine has one; claude/external do not) — that is why a cut script still ships nothing.
